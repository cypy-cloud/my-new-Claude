# FP AI Assistant — Claude 작업 컨텍스트

> 이 파일은 세션이 바뀌어도 Claude가 프로젝트 맥락을 유지하기 위한 파일입니다.
> 새 기능을 추가하거나 중요한 결정이 생기면 이 파일을 업데이트해주세요.

---

## 프로젝트 개요

- **서비스명**: FP AI Assistant (보험설계사 전용 AI 도구)
- **스택**: Next.js 14 App Router (TypeScript), Supabase Auth + DB, Vercel 배포
- **AI 모델**: Anthropic Claude
  - 기본(대부분 기능): `claude-haiku-4-5-20251001`
  - 고품질(스크립트+성향분석 포함, SMS 5번째 초안): `claude-sonnet-5`
- **결제**: 포트원(PortOne) + 한국결제네트웍스(KPN) — 일반결제/정기결제(빌링키) 코드 구현 완료.
  **[2026-07-20] 9개 카드사 중 7개 심사 완료(하나·현대카드 제외), MID/Passkey 수령 완료**
  (7/13 제출, 예상보다 빠름) — 자세한 남은 절차는 "앞으로 할 것" 4번 참고. 승인 불발 대비
  **NHN KCP를 백업 PG로 병행 신청 진행 중**(가입비/연회비 무료 안내 받음) — 포트원이 NHN KCP를
  이미 지원하므로 승인되면 코드 수정 없이 관리자콘솔에서 채널만 추가하면 전환 가능
- **배포 브랜치**: `main` → Vercel 자동 배포

---

## 요금제 (현재 확정)

> **월정액 결제만 지원** — KPN(한국결제네트웍스) 내부 정책상 연간결제 서비스는 PG 입점이
> 불가하다는 안내를 받아, 연간결제/2개월 할인 관련 UI·로직을 전부 제거함 (2026-07-10).

| 플랜 | 월정액 |
|------|--------|
| 무료 | ₩0 |
| 기본 (Basic) | ₩5,900 |
| 프로 (Pro) | ₩9,900 |
| 프리미엄 (Premium) | ₩19,900 |

> **프리미엄 ₩16,900 → ₩19,900 인상 (2026-07-16)** — 팀장(프리미엄 전용)이 팀원(기본 이상)을
> 초대하면 팀원의 초과 사용량이 팀장 한도에서 자동 대여되는 구조(`reserveUsage()`)라, 팀장이
> 실질적으로 "개인 사용자"가 아니라 "팀 운영 인프라"로 기능한다고 판단해 인상. 팀 규모가
> 커져도 팀장 계정 하나의 비용 부담이 무한정 커지지 않도록 대여 총량 캡도 함께 도입함
> (아래 "팀 한도 대여 캡" 참고). 가격 인상은 `lib/subscription/plans.ts` 단일 소스에서
> 관리되고 요금제 페이지·결제 검증·할인코드 계산이 전부 여기서 동적으로 값을 읽어오므로,
> 다른 하드코딩된 가격 참조는 없음을 확인함(2026-07-16 전체 검색 완료).

### 플랜별 기능 한도

| 기능 | 무료 | 기본 | 프로 | 프리미엄 |
|------|------|------|------|---------|
| AI 문자/카톡 | 3 | 15 | 30 | 70 |
| AI 스크립트 통합풀 | 0 | 10 | 20 | 45 |
| AI PDF 분석 (업로드+생성 통합 한도) | 3 | 10 | 30 | 50 |
| SNS·블로그 콘텐츠 | 0 | 0 | 10 | 20 |
| 뉴스레터 | 0 | 0 | 2 | 10 |

> **AI 스크립트 통합풀**은 AI 상담 스크립트·고객 성향분석 2개 기능이 하나의 카운터를 공유함
> (`lib/subscription/usage.ts`의 `'script'` feature)
>
> **뉴스레터 프로 2회**는 의도적인 "맛보기" — 프리미엄의 진짜 차별점은 블로그·SNS 콘텐츠
> 고용량(20 vs 프로 10)이라 뉴스레터를 프로에 소량 개방해도 프리미엄 업셀 유인이 크게
> 줄지 않는다고 판단해 2026-07-13 추가. 뉴스레터 이미지(JPG/PNG) 내보내기 기능의 발행인
> 이름·연락처는 계정 프로필(`profiles.full_name`/`phone`)에 고정되며 패널에서 직접 수정
> 불가 — 계정 하나를 여러 사람이 공유해 각자 다른 이름으로 이미지를 찍어내는 것을 막기
> 위함(`components/newsletter/newsletter-image-panel.tsx`). 이 때문에 회원가입 시 연락처가
> 필수 항목으로 바뀌었고(`components/auth/signup-form.tsx`), 프로필에 연락처가 없는 계정은
> 뉴스레터 이미지 생성이 차단되고 설정 페이지로 안내됨.
> AI 후속연락 추천, 거절극복 스크립트, 재무설계 간이 리포트, AI 상담 후기 작성 기능은 모두 제거됨
> (거절극복은 AI 상담 스크립트 자체의 [OBJECTION] 섹션과 기능이 겹쳐 별도 메뉴로 둘 가치가 낮다고
> 판단, 재무리포트·후기작성은 사용성이 낮고 통합풀 소모만 유발해서 제거)
> **생애주기 알림도 2026-07-06에 메뉴에서 제거됨**(업무 캘린더와 기능 중복, 데이터 축적 시
> 무거워질 위험 — 커밋 `810db85`) — 이후 페이지가 `/dashboard`로 리다이렉트만 하는 빈 껍데기로
> 남아있던 걸 2026-07-20에 완전히 삭제(`app/(dashboard)/lifecycle-alert`, `app/api/ai/
> lifecycle-alert`, `components/ai/lifecycle-alert.tsx` 3개 파일). 별도 DB 테이블이 없던
> 기능이라 데이터 영향 없음, 다른 파일에서의 참조도 없음을 확인 후 삭제.
>
> **AI PDF 분석**은 `pdfUploadLimit`(업로드)과 `pdfAnalysisLimit`(설명자료 생성)이 항상 동일한
> 값으로 설정됨 — PDF 업로드는 로컬 텍스트 추출만 하므로 AI 토큰 비용이 없고(비용은 오직
> "설명자료 생성" 단계에서만 발생), 두 한도를 분리해두면 실제 처리 가능한 PDF 개수가 왜곡되므로
> 항상 같은 숫자로 유지할 것. 추출된 텍스트는 사용자에게 노출하지 않음(미리보기/다운로드 없음) —
> `document_generator`가 업로드된 파일을 선택하면 서버에서 저장된 추출 텍스트를 그대로 프롬프트에 사용.

---

## 수익률 계산 기준 (항상 이 기준으로 계산)

- **환율**: ₩1,550 / USD (고정)
- **서버비**: ₩70,000 / 월 (Vercel + Supabase)
- **결제 수수료**: 3.6% (Toss페이먼츠 신용·체크카드 실제 수수료율, 부가세 별도)
- **AI 모델 비용** (Sonnet 5 할인 중 ~2026-08-31):
  - Sonnet 5: Input $2/1M → ₩3,100/1M / Output $10/1M → ₩15,500/1M
  - Haiku 4.5: Input $0.80/1M → ₩1,240/1M / Output $4/1M → ₩6,200/1M
- **사용 시나리오**: 100% / 70% / 50% 3가지 모두 항상 표시

---

## 초과 크레딧 (추가 건수 구매) 시스템

유료 플랜 전용, 한도 소진 후 추가 구매 가능. 30일 유효.

| 팩 | 금액 | 건당 단가 | 할인 | 배지 |
|----|------|-----------|------|------|
| 10건 | ₩2,000 | ₩200 | 기준 | - |
| 20건 | ₩3,800 | ₩190 | -5% | - |
| 30건 | ₩5,400 | ₩180 | -10% | POPULAR |
| 40건 | ₩6,800 | ₩170 | -15% | - |
| 50건 | ₩8,000 | ₩160 | -20% | BEST |

- DB 테이블: `user_extra_credits`
- 팩 정의 파일: `lib/billing/credit-packs.ts`
- 크레딧 로직: `lib/subscription/usage.ts` (getExtraCredits / consumeExtraCredit / addExtraCredits)
- 구매 API: `app/api/billing/credits/purchase/route.ts`
- 구매 모달: `components/billing/credits-purchase-modal.tsx`

---

## 할인코드(시연회 프로모션) 시스템

시연회(7/23·7/24, 8/3) 참석자 한정 할인코드. 결제 기존 로직은 건드리지 않고 체크아웃
시점에 선택적으로 코드를 입력한 경우에만 참조하도록 별도 테이블로 추가함 (2026-07-13).

| 코드 | 사용 예정일 | 할인율(기본/프로 · 프리미엄) | 기간 | 선착순 | 유효기한 |
|------|------------|---------------------------|------|--------|----------|
| `MET2026` | 7/23, 7/24 | 20% · 30% | 최초 3개월 | 100명 | 2026-08-10 |
| `YM2026` | 8/3 | 20% · 30% | 최초 3개월 | 100명 | 2026-08-10 |

- 두 코드 합쳐 최대 200명, **계정당 1회만** 사용 가능
- 할인은 자동결제(빌링키) 등록 기반 정기결제에만 적용 — 3개월(회차) 지나면 자동으로 정상가 전환
- DB 테이블: `discount_codes`, `discount_code_redemptions` (RLS `using(false)`, 서비스롤 전용)
- 검증/적용 로직: `lib/billing/discount.ts` (validateDiscountCode / redeemDiscountCode / getActiveRedemption)
- 결제 검증(`app/api/billing/verify/route.ts`)에서 클라이언트가 보낸 할인가를 서버에서 재검증 —
  플랜 정가 검증과 동일한 패턴으로 위변조 방지
- 갱신 결제 시 할인 재적용: `app/api/cron/subscription-check/route.ts`에서 `getActiveRedemption`으로
  남은 회차 확인 후 동일 할인율 적용, 성공 시 `incrementRedemptionUsage` 호출
- 체크아웃 UI: `components/billing/portone-checkout.tsx`의 할인코드 입력란

---

## 법적 보호 조치

- [x] **프로그램저작권 등록 신청 완료 (2026-07-16)** — 한국저작권위원회 저작권등록시스템(cros.or.kr)에
      개인 명의로 신청, 결제(53,600원, 신용카드)까지 완료. 접수번호 `2026-043600`. 심사 대기 중
      (형식심사, 통상 1개월 이내). 진행 상황은 cros.or.kr "나의저작권 > 신청진행현황"에서 확인.
  - AI(Claude Code) 활용 개발이라 "생성형 인공지능(GAI) 활용 저작물의 저작권 등록 안내서"
    (문화체육관광부·한국저작권위원회, 2025.6) 기준에 맞춰 신청 — 순수 GAI 산출물이 아니라
    저작자(사용자)가 기획·설계·정책 결정·오류 검증·수정 지시를 직접 수행하고 GAI는 코드
    구현 도구로 활용했다는 점을 "본 프로그램의 특징"란 및 첨부 서류(GAI 활용 내역 설명서)에
    명시함 — 안내서가 요구하는 통제가능성·예측가능성 요건 충족 목적
  - 프로그램 종류: 응용프로그램 > 사무관리 > 기타 사무관리 S/W, 판매구분: 상업용
  - 첨부 자료(스크래치패드에 생성): 프로그램 명세서(.docx), 소스코드 발췌본(.zip —
    script/message route, provider.ts, request-lock.ts, usage.ts, discount.ts 6개 핵심
    파일), GAI 활용 내역 설명서(.docx, ①개요 ②활용된 GAI 산출물 ③인간의 창작적 표현
    부분 3단계 구조)
- [x] **이용약관 v1.2 → v1.3 (2026-07-16)** — 지식재산권(제9조)·금지행위(제10조) 조항 신설.
      기존 약관에 서비스 자체(UI/디자인/소스코드/AI 프롬프트)에 대한 권리 귀속, 크롤링·역공학·
      재판매·계정 공유 금지 조항이 없었던 걸 보완. 자세한 내용은 `app/terms/page.tsx` 참고.
- [ ] **NHN KCP 백업 PG 신청** — 자세한 진행 상황·남은 작업(070 유선전화, 통신판매신고 면제
      여부, 전용 보증보험 200만원, 계약서류 제출, 과세유형 전환)은 "앞으로 할 것" 2·3번 참고
- [ ] 상표 출원 — 시연회 반응 보고 판단하기로 함 (당장 급하지 않음)

---

## 인프라 현황

- [x] Vercel Pro 업그레이드 완료 (2026-07-13) — Hobby 플랜의 상업적 이용 금지 약관 위반 리스크 및
      함수 타임아웃(10초→300초) 문제 해소. 프리미엄 성향분석 스크립트(Sonnet)는 응답까지 약 120초
      걸려 Hobby 플랜에서는 타임아웃으로 실패했을 가능성이 있었음
- [x] **버그 수정(2026-07-15)**: 위 업그레이드 이후에도 실사용자가 "고객성향분석 결과 불러오기"를
      적용해 AI 상담 스크립트를 생성하면 한참 로딩하다가 결과 없이 끝나는 문제 발생. 원인은
      `app/api/ai/script/route.ts`에 `export const maxDuration = 60`이 하드코딩돼 있어서, Vercel
      Pro의 기본 300초 한도와 무관하게 이 라우트만 60초에서 강제 종료되고 있었음 — 성향분석
      연동 시(Sonnet + 16,000토큰 상세 프롬프트) 실측 약 120초가 걸리므로 항상 타임아웃남.
      240초로 상향 수정.
- [x] Supabase Pro 업그레이드 완료 (2026-07-13) — 무료 플랜의 7일 비활성 자동 일시정지 리스크 해소
- [x] Anthropic API 크레딧 자동충전 설정 완료 (잔액 $15 이하 시 $50 자동 충전)
- 100명 규모 기준으로는 Vercel/Supabase Pro 기본 포함 한도 안에서 충분히 커버됨 (자세한 근거는
  대화 기록 참고, 필요 시 재계산 가능)

---

## 포트원(PortOne) 결제 연동 현황

`lib/billing/portone-provider.ts` 에 상세 주석 있음. PG는 한국결제네트웍스(KPN)로 신청,
가입비/연회비 무료 + 빌링·수기 수수료 2.90%(부가세 별도, 계약 확정 전 잠정치).

- [x] 일반결제(요금제/크레딧 1회 결제) — `PortOne.requestPayment()`, 리다이렉트 없이 응답 즉시 확인
- [x] 빌링키 발급/자동청구/삭제 — `verifyBillingKey`/`chargeBillingKey`/`deleteBillingKey`
- [x] profiles 컬럼: `portone_billing_key`, `portone_customer_id`, `billing_card_last4`, `billing_card_brand`
- [x] 구독 만료 크론에서 빌링키 자동 갱신 시도 (`app/api/cron/subscription-check/route.ts`)
- [ ] **PG 계약 승인 후 남은 작업**: Vercel에 `PORTONE_API_SECRET`, `NEXT_PUBLIC_PORTONE_STORE_ID`,
      `NEXT_PUBLIC_PORTONE_CHANNEL_KEY`, `PORTONE_WEBHOOK_SECRET` 실제 값 설정 → 실결제 테스트
- [ ] 확정된 최종 수수료율을 이 문서 "수익률 계산 기준" 절의 3.6%와 대조해 필요 시 갱신

---

## 주요 구현 완료 기능 목록

### AI 기능
- [x] AI 문자/카톡 생성 (Haiku 4가지 + Sonnet 1가지("설득력 있는 버전"), 5가지 스타일).
      **버그 수정(2026-07-16)**: "부드러운 버전"에 "후속 연락용" 내용이 그대로 섞여 나오는
      문제 발생 — `parseOutputSections`가 Haiku 응답(SMS/KAKAO/SOFT/FOLLOWUP 4개 마커)과
      Sonnet 응답(PERSUASIVE 1개 마커)을 하나의 고정 마커 순서로 같이 파싱하다가, Haiku
      텍스트엔 없는 PERSUASIVE 마커를 못 찾아 SOFT 섹션이 텍스트 끝(FOLLOWUP까지)을 통째로
      삼켜버렸음. 각 raw 텍스트를 실제로 생성한 프롬프트의 마커 목록으로만 파싱하도록 수정
      (`app/api/ai/message/route.ts`). 부수적으로 Haiku maxTokens 2500이 4개 섹션 합산
      분량엔 부족해 마지막 섹션이 잘리던 문제도 5000으로 상향해 같이 해결. "설득력 있는
      버전"(Sonnet)도 별도로 문장 중간 잘림 발생 이력 있어 maxTokens 3000→4096 상향, 목표
      분량도 사용자 요청으로 700~1000자→1200~1500자 확대(추가 비용 생성 1건당 약 15~16원
      수준, 무시 가능). **추가 버그 수정(2026-07-16)**: 위 수정 직후 4가지 버전 전부에서
      문단 앞마다 갑자기 ①②③④⑤ 번호가 붙고 말미에 `---`/`##` 마크다운 잔재가 남는 문제
      발생 — 프롬프트의 "5단계 구조" 설명에 원문자 번호(①②③④⑤)를 그대로 써둔 게 원인으로,
      모델이 이를 실제 출력 서식으로 오해해 그대로 베껴 씀. 설명 문구를 원문자 번호 없는
      화살표 흐름으로 바꾸고, 번호·글머리 기호·마크다운 금지를 명시했으며(이 프롬프트에는
      script/newsletter 라우트와 달리 `---`/`##` 금지 지시 자체가 없었음), 파싱 단계에도
      `cleanSectionText` 방어 로직 추가.
- [x] AI 상담 스크립트 생성 (성향분석 포함 시 Sonnet 5, 미포함 시 Haiku)
- [x] 고객 성향 분석 (Sonnet 5, 프로 이상). "기존 고객 불러오기" 드롭다운으로 고객관리에
      등록된 고객을 선택하면 이름·나이대·성별·직업·가족상황이 자동으로 채워짐(2026-07-15).
      고객관리와 성향분석은 값 체계가 달라(연령대 세분화, 소득 구간 경계 등) 안전하게
      대응되는 항목만 자동 채우고, 소득 수준은 구간이 어긋나서 자동 채우지 않음
      (`components/ai/customer-analysis.tsx`의 `handleSelectCustomer`).
- [x] AI PDF 분석 (Haiku, 3개 그룹 병렬 호출)
- [x] 블로그·SNS 콘텐츠 생성 (Haiku, 프로 이상)
- [x] 뉴스레터 생성 (Haiku, 프로 2회/프리미엄 10회). 주제 입력은 자유 타이핑 대신 **대분류 7개
      (건강/의료, 금융/재테크, 보험상식/점검, 생활/시즌이슈, 생활안전/사고예방, 법인/전문직특화,
      고객관계관리·감사콘텐츠) → 중분류(대분류당 4~5개) → AI 주제 제안(최대 3회, 사용량 차감 없음)
      → 선택 또는 직접입력/수정** 흐름으로 개편(2026-07-15) — 신입 설계사의 "뭘 써야 할지 모르겠다"
      문제 해결이 목적. `lib/content/newsletter-topics.ts`(카테고리 데이터),
      `app/api/ai/newsletter/suggest-topics/route.ts`(제안 전용 API — `blockIfLimitExceeded`/
      `incrementUsage`를 호출하지 않아 뉴스레터 생성 한도와 무관하게 재시도 가능, 프론트에서
      3회로 제한). **"계절/시기 이슈" 자유입력 필드는 삭제** — 서버가 오늘 날짜로 자동 계산
      (`lib/content/newsletter-season.ts`)해서 프롬프트에 반영. 실시간 검색(정책/제도 변화 카테고리)은
      웹서치 API 비용($10/1,000회 ≈ 1회당 ₩15.5) 문제로 채택하지 않고 "고객 관계관리·감사 콘텐츠"로
      대체 — 검색 없이도 항상 안전한 비용으로 생성 가능한 카테고리만 채택.
      카테고리만 선택하고 "참고 자료"를 안 채우면 내용이 다소 일반적으로 나올 수 있어 눈에 띄는
      안내 박스 추가(2026-07-15). **발행인 로고/프로필 사진**은 설정 페이지에서 계정당 1장 등록
      → 모든 뉴스레터 이미지 하단 발행인 정보 옆에 자동 표시(이름·연락처와 동일하게 계정 공유
      방지 원칙 적용, 이미지 패널에서는 수정 불가). `profiles.avatar_url` +
      `newsletter-avatars` 공개 스토리지 버킷(RLS: 본인만 업로드/수정/삭제, 전체 공개 읽기 —
      html-to-image가 브라우저에서 직접 불러와야 해서). **[사용자 액션 필요]**
      `supabase/migrations/055_newsletter_publisher_image.sql` 아직 미실행 — Supabase SQL
      Editor에서 실행 전까지는 로고 업로드 시 에러 발생.
- [x] 뉴스레터 이미지(JPG/PNG) 내보내기 — 레이아웃 6종, 폰트 3종, 발행인 정보 계정 고정.
      보관함(내 결과물)에서 재생성 시 사용량 차감 없음(`parseNewsletterSections`로 저장된
      텍스트를 역파싱). 보험 점검 포인트는 항상 정확히 3개, "✓ " 한 줄 후킹 문장으로 고정
      (`app/api/ai/newsletter/route.ts` 프롬프트). 마크다운 기호(#, ##, **, 트레일링 --- 등)는
      프롬프트에서 금지 + 이미지 조립 최종 단계(`newsletter-image-panel.tsx`의
      `clean = stripTrailingRule(stripMarkdown(text))`)에서 한 번 더 방어적으로 제거 —
      과거 생성분을 보관함에서 다시 불러와도 깨끗하게 나옴. CTA에 사과 표현("그동안 안부
      문자로만...") 금지 규칙도 프롬프트에 명시. 컴플라이언스 주의문구는 텍스트/TXT
      다운로드에는 남기되 이미지에는 `stripDisclaimer()`로 제외.
      **프롬프트 수정(2026-07-14) 이전에 저장된 보관함 항목**은 체크포인트 5개·사과 표현이
      원본 그대로 저장돼 있어 재생성해도 그대로 나오는 문제가 있었음 — `limitCheckPoints()`
      (몇 개든 이미지에는 앞 3개 항목 단위만 표시), `stripApologyCta()`("안부 문자"+"죄송/미안"
      패턴 문장만 제거)를 이미지 조립 단계에 추가해 옛 저장분도 이미지에서는 깔끔하게 나오도록
      방어. 원본 저장 텍스트·TXT/DOCX 다운로드는 그대로 유지(컴플라이언스 문구와 동일 원칙).
- [x] 템플릿 라이브러리 19종 (문자 10 + 스크립트 9, 프리미엄 전용 4종 포함) — `content_templates` 테이블
- [x] 오늘의 동기부여 명언 팝업 — 대시보드 진입 시 하루 1회, 날짜 기준으로 전 사용자
      공통 노출(같은 날엔 모두 같은 명언). 검증된 인물 명언 143개 + 직접 작성한
      보험 세일즈 특화 문구 178개, 총 321개 순환(약 11개월 주기). `lib/content/sales-quotes.ts`,
      `components/dashboard/daily-quote-popup.tsx`. 노출 여부는 localStorage로만
      관리 — DB/서버 비용 없음, 기존 로직과 완전 분리된 추가 기능 (2026-07-14).
      **1차 보강(2026-07-17)**: 검증된 인물 명언 30개→55개 — 지그 지글러·W.클레멘트 스톤·
      메리 케이 애시·그랜트 카돈(세일즈 전문가) + 무하마드 알리·마이클 조던·스티브 잡스·
      넬슨 만델라 등 강력한 후킹형 동기부여 인물로 구성.
      **2차 대량 보강(2026-07-17)**: "500개까지 늘리되 유명인 명언 300개로" 요청에 대해,
      숫자를 억지로 채우면 출처 불확실하거나 후킹력 약한 문구가 섞일 위험이 있다고 판단해
      **유명인 150~180개 + 자체작성 150~200개(합계 350~400개 목표)로 절충 제안 후 진행** —
      최종 검증된 인물 143개(니체·아리스토텔레스·소크라테스 등 고전 철학자 22개 + 피터
      드러커·워런 버핏·오프라 윈프리·마틴 루터 킹 등 경영/자기계발 거장 66개 추가), 자체작성
      178개(93개 기존 + 85개 신규)로 마무리. 신규 자체작성 문구는 사용자 요청대로 "출근하기
      싫다가도 나가야겠다는 동기", "고객을 어디서 찾을지 아이디어가 떠오르는" 식의 구체적
      후킹력을 살려서 작성(출근/하루시작·고객발굴 아이디어·슬럼프 극복·자신감·실적 마인드셋·
      하루마무리 6개 카테고리, 각 10~15개). **정확성 유의사항**: 이 규모(143개)에서는 문구
      하나하나를 원전 대조까지는 하지 못했고, 널리 알려지고 통용되는 인용구/번역 기준으로
      선별함 — 아인슈타인·링컨·간디·부처처럼 인터넷에 가짜 명언이 특히 많이 도는 인물은
      의도적으로 제외.

### 고객관리
- [x] **리크루팅 후보 관리 (2026-07-20)** — 경력 설계사의 주요 수입원인 신규 설계사 리크루팅을
      지원하기 위해, 완전히 새로운 화면 대신 기존 고객관리를 확장하는 방식으로 구현(리스크
      최소화 목적). `customers` 테이블에 `contact_type`('customer'|'recruit', 기본값 'customer')
      필드만 추가(`supabase/migrations/057_customer_contact_type.sql`), 기존 행은 전부
      자동으로 'customer'가 되어 기존 동작에 영향 없음. 고객 등록/수정 폼에 구분 토글 추가,
      목록·상세에 뱃지+필터 추가.
      **AI 문자/상담 스크립트 자동 전환**: 고객관리에서 "리크루팅 후보"를 불러와 AI 문자
      (`/ai-message?customerId=`) 또는 상담 스크립트(`/ai-script?customerId=`)를 생성하면,
      `contact_type`이 자동으로 감지되어 프롬프트가 "보험 상품 제안"에서 "보험설계사 커리어
      제안"으로 전환됨 — 기존 "목적"/"상품 분야" 입력란을 그대로 재사용하되 리크루팅 모드일 땐
      선택지·라벨만 바뀜(예: "상품 분야"→"제안 포인트": 수입구조/시간자유도/자기계발/여성친화적
      근무환경 등). 상품 카테고리 선택기·보험 관련 고지문은 리크루팅 모드에서 자동으로 숨김/생략.
      스크립트는 기존 10개 섹션 마커(PREP~FOLLOWUP) 구조를 그대로 유지하고 내용만 리크루팅
      제안 상담으로 전환(`buildRecruitScriptPrompt`) — MBTI 성향분석 연동은 리크루팅에는
      아직 없어 항상 lite(Haiku) 버전만 사용.
      **안전장치**: 일반 고객(`contact_type='customer'`, 기본값) 경로는 분기 조건이 전부
      `isRecruit === false`로 귀결되어 기존 코드와 완전히 동일한 로직을 그대로 타도록 설계 —
      새 기능 추가가 기존 문자/스크립트 생성 기능에 영향을 주지 않음(코드 리뷰로 확인,
      `tsc --noEmit`+`next build` 통과). **[x] 마이그레이션 실행 완료 (2026-07-20)** — Supabase
      SQL Editor에서 직접 실행, "Success. No rows returned" 확인. 실사용자 클릭 테스트로
      정상 작동 확인(2026-07-20 스크린샷).
      **보강 1: 리크루팅 모드 수동 전환 (2026-07-20)** — 기존엔 고객관리에서 리크루팅 후보를
      불러왔을 때만(`customerId` URL 파라미터) 리크루팅 모드가 켜졌는데, 고객을 아예 안
      불러온 빈 화면(`/ai-message`, `/ai-script` 직접 진입)에서는 리크루팅 문구를 만들 방법이
      없었음(사용자 피드백으로 발견). `isRecruit`을 파생값이 아닌 상태로 바꾸고, "문자 종류"/
      "스크립트 종류" 토글(일반 고객 ↔ 리크루팅)을 화면 상단에 직접 노출 — 고객을 불러왔으면
      그 구분을 기본값으로 쓰되 언제든 수동으로 전환 가능. 전환 시 목적/제안 포인트 입력값은
      두 모드의 선택지 목록이 달라 초기화(잘못된 값이 남아있는 걸 방지).
      **보강 2: 고객관리에 MBTI 저장 (2026-07-20)** — 기존엔 "고객 성향분석" 화면에서 MBTI를
      매번 새로 입력해야 했음(고객관리 DB에 MBTI 필드 자체가 없었음). `customers` 테이블에
      `mbti_type`(16유형 체크 제약, nullable) 추가(`058_customer_mbti_type.sql`), 고객 등록/
      수정 폼에 MBTI 선택란 추가(모르면 QR 간이검사 안내). "고객 성향분석" 화면의 "기존 고객
      불러오기"가 이제 MBTI도 함께 자동으로 채워줌 — 두 화면이 같은 16유형 체계를 쓰므로
      별도 값 변환 없이 그대로 매핑. `MBTI_TYPES` 상수는 `types/index.ts`로 단일화해서
      customer-form.tsx/customer-analysis.tsx가 같은 목록을 공유(기존엔 각자 따로 정의돼
      있었음). **[x] 마이그레이션 실행 완료 (2026-07-20)** — Supabase SQL Editor에서 직접
      실행, "Success. No rows returned" 확인. MBTI 저장 기능 실제로 사용 가능한 상태.
      **보강 3: 고객 MBTI → 문자/스크립트 생성 자동 연동 (2026-07-20)** — 사용자가 실사용
      테스트 중 "고객관리에서 MBTI를 저장해뒀는데 문자/스크립트 생성에 실제로 반영된 게
      맞냐"고 질문해서 코드를 재확인한 결과, 보강 2에서 저장한 `customers.mbti_type`이
      AI 문자/스크립트 생성 화면의 고객 자동입력(`ai-message`/`ai-script` page.tsx의
      select 쿼리)에는 애초에 포함되어 있지 않았고, 문자/스크립트의 MBTI 반영은 오직
      "고객성향분석 결과 불러오기"로 **별도로 저장해둔 심층 분석**을 수동으로 불러왔을
      때만(`[고객성향분석 결과]` 마커) 동작하는 완전히 분리된 두 기능이었음을 확인 — 즉
      고객등록에서 MBTI만 선택해둔 상태로는 자동 반영이 안 되고 있었음. 사용자 요청으로
      "고객등록에서 MBTI를 선택해두면, 그 고객을 불러와 문자/스크립트를 생성할 때 무조건
      자동으로 반영"되도록 연동 추가. 다만 심층 분석 승격(Sonnet, ~16,000토큰, 120초+)까지
      자동으로 트리거하면 문자 하나 만드는데 비용·시간이 과하게 커지므로, **MBTI 코드만
      있을 때는 가벼운 톤 조정 지침만 추가하는 방식으로 절충**(사용자에게 A/B 두 안 제시
      후 A안으로 진행 확정) — 모델은 기존과 동일(Haiku), 속도·비용 변화 없음. 심층 분석이
      함께 로드된 경우엔 기존처럼 더 상세한 `buildPersonalityAddendum`/
      `buildAnalysisScriptPrompt`가 우선 적용되고 이 가벼운 지침은 쓰이지 않음(중복 방지).
      리크루팅 후보(`contact_type='recruit'`)도 동일하게 적용 — MBTI는 상담 목적과 무관한
      개인 성향이라 일반 고객/리크루팅 구분 없이 적용하는 게 맞다고 판단.
      구현: `app/(dashboard)/ai-message,ai-script/page.tsx`의 select에 `mbti_type` 추가해
      `initialData.mbtiType`으로 전달 → `message-generator.tsx`/`script-generator.tsx`의
      `buildParams()`가 `mbtiType`을 API 페이로드에 포함 + 고객 자동입력 배너에
      "MBTI(◯◯◯◯)에 맞춰 문체가 자동 조정됩니다" 안내 표시 → `app/api/ai/message,script/
      route.ts`에 `buildMbtiAddendum(mbti)` 추가, `hasAnalysis`가 false일 때만(중복 방지)
      각 프롬프트 빌더에 주입, 캐시 키(`cacheInput`)에도 `mbti` 포함해 MBTI 유무에 따라
      다른 캐시로 분리. 실사용자가 박정수 고객으로 일반 문자·리크루팅 문자·리크루팅
      스크립트 3가지 모두 생성해 ISFJ 톤 반영을 직접 확인함(2026-07-20) — Haiku가 프롬프트의
      가벼운 E/I·T/F·J/P 힌트를 넘어 "ISFJ 성향상"이라고 스스로 명시하며 모델이 원래 갖고
      있는 MBTI 지식까지 자연스럽게 얹어 반영하는 것을 확인.
      **보강 4: AI 문자/스크립트 생성 화면에 고객 직접 불러오기 추가 (2026-07-20)** — 위
      보강들은 전부 고객 상세 페이지에서 "문자 생성"/"상담 스크립트 생성" 버튼으로
      진입(`?customerId=`)했을 때만 고객 정보가 자동으로 채워졌고, `/ai-message`·`/ai-script`에
      메뉴로 직접 들어가면 등록된 고객을 불러올 방법이 아예 없었음(사용자가 실사용 중 발견).
      `customer-analysis.tsx`의 "기존 고객 불러오기" 드롭다운과 동일한 패턴을 그대로
      재사용 — 마운트 시 `/api/customers`로 전체 목록을 불러와 두 생성기 화면에 동일한
      드롭다운을 추가하고, 선택 시 이름·연령대·직업·관계(또는 성별·결혼/자녀 여부·소득
      수준)·관심상품·리크루팅 구분·MBTI까지 한 번에 채움(`handleSelectCustomer`). 고객관리
      폼(`customer-form.tsx`)의 결혼여부·자녀여부·소득수준 옵션 값이 스크립트 생성기와
      동일한 상수를 쓰는 걸 확인해 별도 값 변환 없이 그대로 매핑 가능했음. `initialData?.mbtiType`을
      직접 읽던 부분은 전부 `mbtiType` 로컬 state로 전환해 드롭다운 선택 시에도 갱신되도록 함.
      URL로 `customerId`가 이미 넘어온 경우(고객 상세→버튼 진입)에도 드롭다운은 계속 표시되어
      언제든 다른 고객으로 바꿔 부를 수 있음.
      **버그 수정(2026-07-20, 같은 날 코드 리뷰로 발견)**: `handleSelectCustomer`가 (1)"직접
      입력"으로 되돌려도 이전에 불러온 고객 정보를 초기화하지 않고, (2)다른 고객으로 전환해도
      필드별로 `if (c.field) setX(...)`처럼 값이 있을 때만 채워서 새 고객에게 없는 필드는
      이전 고객 값이 그대로 남아 두 고객 정보가 섞이며, (3)"고객성향분석 결과 불러오기"로
      불러온 심층분석이 고객·모드를 바꿔도 해제되지 않아 무관한 내용이 새 메시지에 섞여
      들어갈 수 있었음 — 세 가지 모두 수정(항상 전체 필드를 새로 채우도록 변경 + 고객/모드
      전환 시 성향분석 자동 해제). `customer-analysis.tsx`의 기존 "기존 고객 불러오기"에도
      (1)(2)와 동일한 패턴이 남아있음을 확인, 사용자 요청으로 같은 방식(항상 전체 필드를
      새로 채우도록)으로 함께 수정 완료.

### 결제/구독
- [x] 요금제 페이지 (기본/프로/프리미엄)
- [x] 포트원(PortOne) 일반결제/빌링키 연동 구조 (PG 계약 승인 전)
- [x] 초과 크레딧 5개 팩 구매 플로우
- [x] 70%/90% 사용량 경고 배너
- [x] 한도 초과 배너 + 크레딧 구매 버튼

### UX
- [x] 음성 입력 (AI 문자, 스크립트, 성향분석, PDF 설명자료, 블로그 핵심메시지)
- [x] 내 결과물 보관함 저장 (스크립트, 콘텐츠 등)
- [x] 대시보드 사용량 카드 (플랜별 기능 자동 표시)
- [x] 플랜 잠금 (PlanGate 컴포넌트)

### 인프라
- [x] Supabase DB 마이그레이션 (usage_records, user_extra_credits 등)
- [x] 어드민 페이지 (사용자 관리, 빌링, CSV 내보내기)
- [x] 팀 관리 기능 — 팀 생성(팀장)은 **프리미엄 전용**, 팀원 연결은 **기본 플랜 이상만** 가능
      (2026-07-15 수정 — 이전엔 플랜 체크가 전혀 없어 무료 회원도 팀을 만들고 초대할 수 있던 버그).
      **팀원 한도 대여**: 팀원이 본인 월 사용량을 다 쓰면, 소속 팀장의 남은 한도에서 자동으로
      대신 차감됨(`lib/subscription/usage.ts`의 `reserveUsage()`) — **문자/스크립트(성향분석
      포함)/PDF분석 3개만** 대상. 콘텐츠·뉴스레터는 대여 대상에서 제외(2026-07-15) — 기본
      플랜은 이 두 기능이 원래 0회(프로 이상 전용)라 대여를 허용하면 요금제 등급이 무력화되고,
      뉴스레터는 발행인 이름·연락처가 항상 생성한 본인 것으로 찍히는 특성상 팀장 한도로 팀원
      개인 브랜딩 뉴스레터를 만드는 것도 맞지 않다고 판단. 팀장 본인 한도까지 소진되면 그때는
      정상적으로 한도 초과 처리.
      생성된 결과물은 차감 주체와 무관하게 항상 실제 생성한 팀원 본인의 "내 결과물 보관함"에
      저장됨. `getMonthlyUsage()`/`getCurrentUserPlan()`은 RLS(`auth.uid()=user_id`) 우회를 위해
      admin client로 조회하도록 수정 — 팀원 세션에서 팀장의 사용량을 확인해야 하는 이 기능
      특성상 필요했음(기존 "본인 조회" 호출부는 결과 동일, 하위 호환 확인됨). 기존에 이미
      생성된 팀/팀원은 소급 적용되지 않고 그대로 유지됨(신규 생성·연결 건부터 적용).
      **팀 한도 대여 캡 (2026-07-16 추가)**: 기존엔 팀 규모와 무관하게 팀원 전체가 팀장의
      남은 개인 한도를 전부 소진할 수 있었음(팀장 본인 몫이 하나도 안 남을 수도 있는 구조).
      이를 보완해 "팀 전체가 이번 달에 팀장에게서 빌려갈 수 있는 총량"에 별도 캡을 둠 —
      **문자 20건 / 스크립트 10건 / PDF분석 15건 (월간, 팀원 전원 합산)**. 팀장 본인 한도
      (프리미엄 기준 70/45/50)의 약 25~30% 수준으로, 팀장 본인 사용량을 최소 50/35/35건은
      항상 보장하는 선. 캡 소진 후 추가로 요청하는 팀원은 기존과 동일하게 "한도 초과" 처리되어
      본인 크레딧 구매·플랜 업그레이드로 유도됨(방어 목적 이상의 부가 수익 전환 효과).
      새 테이블 `team_usage_loans`(팀별·월별 대여량 집계, RLS `using(false)` 서비스롤 전용
      — discount_codes와 동일 패턴)과 RPC `increment_team_loan()`으로 구현
      (`supabase/migrations/056_team_usage_loan_cap.sql`). `reserveUsage()`가 대여 결정 시
      캡을 함께 확인하고, `incrementUsage()`가 실제 생성 성공(캐시 아님) 시점에만 대여량을
      1 증가시킴 — usage_records 차감 시점과 동일하게 맞춰 캐시된 응답이 캡을 소모하지
      않도록 함. 캡 상수는 `lib/subscription/usage.ts`의 `TEAM_LOAN_CAPS`. 캡 조회/기록 쿼리는
      이 프로젝트의 기존 관례대로 Supabase 에러를 던지지 않고 조용히 무시하도록 작성돼 있어
      (`{ data } = await ...`로 error 미확인, 다른 함수들과 동일 패턴), 마이그레이션 미실행
      상태에서도 팀원 한도 대여 자체가 깨지지는 않음 — 다만 그 경우 캡이 그냥 적용되지
      않고(항상 0건으로 조회됨) 기존처럼 무제한 대여로 동작함.
      **[x] 마이그레이션 실행 완료 (2026-07-17)** — Supabase SQL Editor에서 직접 실행,
      "Success. No rows returned" 확인. 캡이 실제로 적용되는 상태.
- [x] 백업 로그 시스템

---

## 앞으로 할 것 (우선순위 순)

> ### ★ 시연회(7/23·7/24·8/3) 종료 후 사용자가 꼭 확인할 것 (2026-07-17 기록)
> 아래 항목들은 시연회가 다 끝난 뒤 한 번에 정리하기로 한 것들 — 잊지 말고 이 목록부터
> 다시 볼 것:
> 1. **할인코드(MET2026/YM2026) 사용 현황 점검** — 코드당 선착순 100명, 유효기한
>    2026-08-10까지. 시연회 끝나고 실제로 몇 명이나 코드를 썼는지, 100명 소진 여부를
>    확인해볼 것. **[2026-07-21 정정]** 이걸 보여주는 관리자 화면이 실제로는 없음(이전
>    기록이 잘못됨) — 지금은 Supabase SQL Editor에서
>    `select code, redemption_count, max_redemptions from public.discount_codes`로 직접
>    조회해야 함. 여유 생기면 결제 관리 화면에 이 표를 추가하는 것도 고려
> 2. **NHN KCP 백업 트랙 재검토** — 070 인터넷전화·전용 보증보험(200만원)·계약서류 제출을
>    일시중단해둔 상태. KPN 최종 결과(하나·현대카드 포함)가 다 나왔는지 확인하고, 이어서
>    진행할지 완전히 접을지 결정 (자세한 내용은 "앞으로 할 것" 2번 참고)
> 3. **상표 출원 여부 결정** — 시연회 참석자 반응을 보고 판단하기로 했던 것 (급하지 않음)
> 4. **서브에이전트 자동화 착수** — 스모크 테스트 봇, 결제/구독 이상 탐지, 사용량·매출
>    정기 리포트, 피드백 자동 분석 파이프라인 (자세한 내용은 "앞으로 할 것" 7번 참고)
> 5. **재사용 가능한 "스타터 템플릿" 저장소 분리 착수** (자세한 내용은 "앞으로 할 것"
>    8번 참고)
> 6. **시연회에서 나온 실사용자 피드백·버그 제보 정리** — 전체 기능 점검(6번 항목)과
>    함께 한 번에 검토
> 7. **사업자등록 겸업사업자 전환 재개** — 세무사 상담 예약·진행 (자세한 내용은 "앞으로 할 것"
>    3번 참고, 2026-07-21 보류 결정)

1. **[검증 대기] 일정 알림(푸시) 안 오는 문제 — 2026-07-14~15에 걸쳐 원인 7개를 순서대로 발견·수정.**
   실제 발송 테스트를 반복하며 원인이 하나씩 드러난 케이스라, 최종 진짜 원인과 함께 전부 기록:
   - **VAPID 키**: Vercel Production 환경변수에 `NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/
     `VAPID_EMAIL` 3개 모두 정상 등록되어 있음을 대시보드에서 직접 확인(2026-07-15) — 처음 의심했던
     이 원인은 아니었음. 키 분실 시 `node -e "console.log(require('web-push').generateVAPIDKeys())"`로
     재생성 가능(재생성하면 기존 구독 전부 무효화되므로 최후의 수단으로만).
   - **크론 주기**: 하루 1회(오후 6시 KST)뿐이라 그 전 시간대 알림은 원천적으로 못 갔던 문제 —
     처음엔 15분 간격으로 수정했으나, "10분 전" 같은 짧은 알림 설정과 어긋나 실제 발송이
     마감 직전 2~3분 전으로 밀리는 오차가 있어 **1분 간격(`* * * * *`)으로 재조정**
     (Vercel Pro 플랜이라 분 단위 크론 가능, `vercel.json`).
   - **9시간 시차 버그**: `app/api/cron/push-notify/route.ts`에서 `due_date`+`due_time`을 시간대
     표기 없이 `new Date()`로 파싱하면 Vercel 서버(UTC) 기준으로 해석돼, 오후 2:30 설정이 서버에는
     밤 11:30(UTC 14:30)으로 잘못 인식되던 문제. `+09:00` 오프셋 명시로 수정 완료.
   - **구독 토글이 꺼져 있었음**: 사용자가 실제로 설정 페이지에서 "일정 알림" 토글이 꺼진 상태였음
     — 다시 켜서 구독 재생성.
   - **★ 진짜 최종 원인(2026-07-15)**: `notify_before_minutes`(몇 분 전에 알릴지)가 **일정 생성/수정
     API(`app/api/tasks/route.ts` POST, `app/api/tasks/[id]/route.ts` PATCH)에서 아예 저장 필드
     목록에 빠져 있어서, 프론트엔드는 정확히 값을 보내는데 서버가 계속 버리고 항상 NULL로만
     저장하고 있었음**. 크론은 `notify_before_minutes IS NOT NULL` 조건으로 대상을 찾기 때문에,
     위 4가지를 전부 고쳐도 애초에 알림 대상 자체가 하나도 없어 발송이 안 됐던 것 — 이게 처음부터
     끝까지의 진짜 근본 원인. TASK_SELECT에도 추가해서 수정 폼 재오픈 시 기존 알림 설정이 안 보이던
     문제도 같이 해결. 부수적으로 PATCH에서 `customer_id`("고객 미연결" 선택 시 빈 문자열)를 그대로
     DB에 보내 "invalid input syntax for type uuid" 에러가 나던 버그도 함께 발견·수정(생성 흐름은
     이미 `|| null` 정규화가 있었는데 수정 흐름에만 빠져 있었음).
   - 아이폰은 **"홈 화면에 추가"로 설치한 상태에서만** 푸시 알림이 동작함(iOS 자체 제한, Safari
     탭으로 열면 절대 안 됨) — 사용자에게 매번 안내 필요.
   - **6번째 원인(2026-07-15)**: 위 5가지를 전부 고친 뒤에도 실사용자가 재테스트 2회 모두 알림을
     못 받았음. 원인은 크론 실행 주기(15분 간격, `*/15 * * * *` → :00/:15/:30/:45에만 실행)와
     알림 가능 구간의 폭 차이 — "10분 전" 같은 짧은 알림을 설정하면 `[notifyAt, dueAt]` 구간이
     15분보다 좁아서, 크론이 그 구간을 완전히 비껴가면(예: 구간이 12:03~12:13인데 크론은
     12:00/12:15에만 실행) `now > dueAt`이 돼버려 영원히 스킵되고 다시는 안 옴. `notify_before_minutes`
     저장은 정상 동작해도 발송 자체가 크론 타이밍에 의해 누락될 수 있었던 구조적 문제.
     `app/api/cron/push-notify/route.ts`에서 마감 시각 이후 30분 유예(`GRACE_MS`)를 두어 다음
     크론에서 반드시 잡아내도록 수정 — 정시보다 늦게 오더라도 아예 안 오는 것보다 낫다는 판단.
     발송 문구도 설정값(`notify_before_minutes`) 대신 발송 시점에 실제 남은 시간을 계산해서
     표시하도록 변경(유예 시간 때문에 "10분 후"라고 보냈는데 이미 지난 경우를 방지).
   - **★★ 진짜진짜 최종 근본 원인(2026-07-15) — 크론 요청이 미들웨어에서 /login으로
     리다이렉트되고 있었음**: 위 6가지를 전부 고치고도 재테스트 2회 모두 실패해서 사용자가
     요청한 Vercel Cron Jobs 실행 로그를 확인한 결과, `/api/cron/push-notify` 요청이 매번
     `GET 307`(리다이렉트)로 찍히고 있었음 — 즉 핸들러 코드 자체가 한 번도 실행된 적이
     없었음. 원인은 `proxy.ts`의 로그인 여부 체크: Vercel Cron이 보내는 요청은 브라우저
     세션 쿠키가 없으므로(각 크론 라우트는 대신 `Authorization: Bearer <CRON_SECRET>`로
     자체 인증하도록 설계돼 있었음) 미들웨어가 매번 `user === null`로 판정해 `/login`으로
     307 리다이렉트시키고 있었고, 그 요청은 실제 알림 발송 로직에 도달하지도 못했음. 위
     6가지 원인은 전부 실재하는 버그였지만, 이 미들웨어 문제 때문에 애초에 그 코드가 실행될
     기회 자체가 없었던 것 — 이게 처음부터 끝까지의 최종 근본 원인. `proxy.ts`에서
     `/api/cron/*` 경로는 세션 체크를 건너뛰고 각 라우트 자체 인증에 맡기도록 수정.
     **주의**: `/api/cron/subscription-check`(구독 갱신·빌링키 자동청구)와
     `/api/cron/cleanup-files`도 동일한 미들웨어를 거치므로 똑같이 실행되지 않고 있었을
     가능성이 높음 — 결제 PG 승인 후 실결제 테스트 시 구독 자동갱신이 정상 동작하는지
     반드시 별도 확인 필요.
   - 위 7가지 모두 수정 완료 상태로 실사용자가 재테스트 예정 — 결과 확인 대기. 기존 테스트
     일정은 이미 알림 구간이 지나 재발송되지 않으니, 재테스트 시 반드시 새 일정으로 할 것.
     Vercel Cron Jobs 로그에서 이제 `GET 200`(더 이상 307 아님)으로 찍히는지, 응답 바디의
     `sent`/`notified` 값이 몇인지로 정상 실행 여부를 1차 확인 가능.
2. **[일시중단, 2026-07-17 결정] NHN KCP 백업 PG — 신청은 접수된 채로 그대로 두고, 추가 투자는 보류.**
   KPN이 9개 카드사 중 7개 승인(하나·현대카드만 남음)까지 진행되면서 백업 PG의 필요성이
   크게 낮아졌다고 판단 — NHN KCP 신청 자체를 취소하지는 않지만(이미 접수된 상태를 유지하는
   덴 비용이 안 듦), **070 인터넷전화 개통·전용 보증보험(200만원) 가입·계약서류 제출 같은
   추가 시간/비용 투자는 지금 중단**하기로 함. KPN 최종 결과(하나·현대카드 포함)가 나오거나
   시연회(7/23·7/24·8/3) 이후 여유가 생기면 재검토. 아래는 중단 시점까지 파악된 내용
   (재개할 경우 참고용으로 남겨둠):
   포트원 관리자콘솔(admin.portone.io)에서는 KPN·NHN KCP 둘 다 "신청 접수 및 계약 진행 중 —
   카드사 심사 요청/진행중" 상태로 확인됐지만(테스트 채널 3개 구성 완료), 실제 NHN KCP가
   2026-07-09에 보낸 이메일(Gmail 확인) 기준으로는 **"카드사 등록심사 요청"을 하기 위한
   사전 체크리스트 7개**가 별도로 있고, 포트원 대시보드 상태와 무관하게 이게 충족돼야
   상점관리자(partner.kcp.co.kr)에서 실제 카드사 등록 신청 버튼을 누를 수 있는 구조.
   `Grep(app/page.tsx 등)`로 코드에서 직접 확인한 결과:
   - ✅ 상호(열린교육컨설팅)·대표자명("대표: 임현수")·사업자등록번호(259-98-01495)·
     사업장주소는 메인 페이지(`app/page.tsx`) 푸터에 모두 정상 표기됨
   - ❌ **유선전화번호 미비** — 현재 푸터에 적힌 010-3949-1525는 휴대폰 번호라 "유선전화"
     요건 불충족 가능성 높음. **070 인터넷전화 신규 개통 필요** (세종텔레콤/스마트070/
     myLGU+ 070 등 온라인 즉시 개통, 월 몇천 원). 번호 확보되면 `app/page.tsx` 푸터에 반영.
   - ⚠️ **통신판매신고번호 없음** — 다만 메일 원문에 "통신판매신고 면제사업자의 경우 예외"
     조항이 명시돼 있음. 면제 요건은 ①간이과세자 또는 ②직전년도 통신판매 거래횟수 50회
     미만. 지금은 실결제가 아직 발생 전이라 ②번 요건으로 면제 대상일 가능성이 높음 —
     정식 통신판매업 신고(PG 계약 완료 후 "구매안전서비스 이용확인증" 첨부 필요) 없이도
     이 항목은 통과 가능성 있다고 판단.
   - 나머지 3개 항목(상품가격·설명 완비, 실결제창 노출, 노출가=결제가 일치, 비회원 구매
     가능 여부)은 코드로 판단 불가 — 사용자가 직접 사이트를 방문자 관점에서 확인 필요.
   - **NHN KCP 전용 이행보증보험(200만원, 계약명 "NHN KCP 전자지불서비스 이용계약") 미가입**
     — 기존에 가입한 보증보험은 KPN 전용이라 별도임(2026-07-16 사용자 확인). 서울보증보험
     마포지점(1599-5209)에서 가입 필요.
   - **계약서류 온라인 제출 여부 미확인** — partner.kcp.co.kr 로그인 → 상점정보관리 →
     계약정보 → 계약동의/본인인증/구비서류제출이 완료됐는지 사용자 확인 필요.
3. **[보류 결정, 2026-07-21] 사업자등록 과세유형 전환(겸업사업자) — 시연회 이후로 연기.**
   사업자등록증(259-98-01495)이 "정보통신업 – 응용 소프트웨어 개발 및 공급업" 종목 추가에도
   불구하고 여전히 "부가가치세 면세사업자"로 표기되어 있던 문제 — **종목 추가와 과세유형(면세
   →과세) 전환은 별개 절차**라 자동 전환되지 않았던 것으로 확인(사업자등록증 스캔본 대조).
   AI 구독료 매출은 과세 대상(정보통신업)이지만 강의(교육 및 강의, 업종코드 940903) 등
   기존 면세 업종은 그대로 유지해야 해서, "면세사업자 → 전부 과세" 전환이 아니라 **겸업사업자
   (과세+면세 동시 보유) 전환**이 맞는 요청임. 홈택스(hometax.go.kr) 온라인 "사업자등록
   정정신고(개인)"으로 신청(접수번호 `312-2026-2-505370016512`, 2026-07-16 접수) → **처리완료로
   떴지만 등록증은 여전히 "면세사업자" 그대로**임을 2026-07-20 확인 — 온라인 민원 자유서술란
   텍스트만으로는 과세유형 자체가 자동 반영되지 않는 케이스였음.
   **2026-07-21 천안세무서 직접 방문 결과**: 담당자로부터 "겸업사업자로 전환하면 사업자등록
   번호 자체가 바뀐다"는 안내를 받음. 그런데 이미 이 번호(259-98-01495)로 **KPN 카드사
   심사·MID 발급·PortOne 채널 등록이 전부 완료된 상태**라, 지금 번호를 바꾸면 결제 연동이
   깨져 PG 재심사(그동안 2주+ 소요)가 필요해질 위험이 큼 — 하필 시연회(7/23·24, 8/3)를
   코앞에 둔 시점이라 리스크가 매우 큼. **그래서 겸업사업자 전환은 시연회 이후로 완전히
   보류하기로 결정** — 지금 사업자등록번호·PG 상태를 그대로 유지한 채 시연회·결제 연동을
   먼저 안정화하는 것을 우선순위로 둠. 당장 번호를 안 바꿔도 즉시 위법은 아닐 가능성이
   높다고 판단(부가세는 분기별 신고라 실제 신고 기한까지는 여유가 있을 것으로 추정) —
   다만 이건 추정이라 확정 아님.
   **[시연회 이후 사용자 액션 필요]**
   - 세무사 상담 진행 — 확인할 것: (1) 정말 번호가 무조건 바뀌는지, 번호 안 바뀌는 다른
     방법은 없는지 (2) 지금 전환 안 하면 정확히 언제까지 괜찮은지(실제 신고 기한)
     (3) 간이/일반과세자 중 어디에 해당하는지, 요금제 가격(₩5,900/9,900/19,900)이
     부가세 포함가인지도 이때 함께 확정
   - KPN 담당자(장석영 님)에게 "나중에 사업자등록번호가 바뀌면 그때 절차·재심사 소요기간이
     어떻게 되는지" 미리 문의해두기 — 실제 전환 시점에 또 놀라지 않도록 사전 정보 확보 차원
   - 위 확인 끝나면 전환 시점을 계획적으로 정해서 재신청
4. **[진행중, 2026-07-20] KPN 카드사 심사 — 9개 중 7개 카드사 심사 완료, MID/Passkey 수령 완료.**
   KPN 담당자(김현아)로부터 "하나·현대카드를 제외한 7개 카드사 모두 심사 완료. 상기 두 카드를
   제외한 카드로 결제 테스트 진행 부탁드립니다" 메일 수신 — 7/13 제출, 최장 2주 예상했던 것보다
   빠르게 진행됨. **[x] 2026-07-20(월요일) 예정대로 KPN(eComm영업팀)으로부터 가맹점 관리자페이지
   (firstpay.co.kr) MID/Passkey 메일 수신 완료** — 매출확인·취소 조회용 KPN 자체 관리자 계정이며,
   **실제 값은 보안상 이 문서에 기록하지 않음**(사용자가 안전한 곳에 별도 보관). 이 메일과는
   별개로 실제 서비스 연동에 필요한 **PortOne API 키는 admin.portone.io에서 별도 확인 필요** —
   아래 절차 계속 진행:
   1) admin.portone.io에서 KPN 채널의 **실운영(라이브)** 키 확인 (테스트 채널 키가 아님 —
      혼동 주의)
   2) Vercel에 실제 값 설정: `PORTONE_API_SECRET`, `NEXT_PUBLIC_PORTONE_STORE_ID`,
      `NEXT_PUBLIC_PORTONE_CHANNEL_KEY`, `NEXT_PUBLIC_PORTONE_BILLING_CHANNEL_KEY`,
      `PORTONE_WEBHOOK_SECRET` — 사용자가 Vercel Personal Access Token을 곧 제공할 예정이라
      그때 직접 설정 예정 (2026-07-16 대화에서 권한 위임 논의 완료)
   3) **하나·현대카드를 제외한** 카드로 실제 결제 테스트 진행 — ⚠️ 이건 샌드박스가 아니라
      **실제로 청구되는 진짜 결제**이므로, 가장 저렴한 플랜(기본 ₩5,900 또는 크레딧 10건
      ₩2,000)으로 테스트 후 정상 처리(구독 반영/영수증) 확인, 필요 시 환불 절차도 같이 확인
   4) 테스트 정상 확인되면 KPN 담당자에게 회신
   **[확인 완료, 2026-07-21] MID 결제방식 — 정기결제(빌링키) 전용, 단건결제 불가로 최종 확인.**
   크레딧 단건결제 실패 원인을 KPN(장석영 님)에게 문의한 결과, 최초 답변("단건/정기 다
   가능")과 다르게 재안내 메일이 옴: "전달드린 MID(빌링)는 정기결제만 이용 가능하며,
   단건결제 이용을 원하시면 MID(인증)을 별도로 추가 발급받아야 함(카드사 재심사 필요,
   통상 10일~2주 소요)". 시연회 전까지 재심사 완료가 불가능해 **단건 MID 신청은 시연회
   이후로 미루기로 결정** — 지금은 KPN 회신만 없이 진행.
   **[완료, 2026-07-21] 신규 구독/업그레이드를 자동결제(빌링키) 방식으로 코드 전환.**
   기존 코드는 신규 요금제 가입·업그레이드의 첫 결제를 전부 일반(단건)결제로 처리하도록
   돼있어, 이 MID로는 크레딧 구매뿐 아니라 **신규 고객의 첫 구독 자체가 막혀있는 상태**였음
   — 시연회 직후 실사용자가 몰릴 걸 감안하면 방치할 수 없는 문제라 코드를 수정함:
   - `app/api/billing/subscribe/route.ts` (신규) — 이미 등록된 자동결제 카드가 있으면
     재사용, 없으면 클라이언트가 새로 발급받은 빌링키를 검증·저장한 뒤 `chargeBillingKey`로
     그 자리에서 즉시 첫 달 요금 청구. 성공 시 `activateSubscription`/`recordPayment`/
     할인코드 처리까지 기존 `/api/billing/verify`의 업그레이드 분기와 동일한 로직 재사용.
     금액은 클라이언트 값을 믿지 않고 서버에서 할인코드 재검증 후 재계산(기존 원칙 유지).
   - `app/(dashboard)/billing/checkout/billing-key/page.tsx` +
     `components/billing/billing-key-checkout.tsx` (신규) — `PortOne.requestIssueBillingKey`로
     카드를 등록(기존 `portone-card-register.tsx`와 동일 패턴)한 뒤 위 라우트를 호출하는
     새 체크아웃 화면. 이미 등록된 카드가 있는 사용자는 카드 등록 단계 없이 "결제하기"만
     누르면 바로 결제됨(크레딧 구매 모달의 `hasBillingKey` 분기와 동일한 UX).
   - `components/billing/upgrade-button.tsx` — 업그레이드 클릭 시 기존 일반결제 체크아웃
     (`/billing/checkout/portone`, 이 MID로는 항상 실패) 대신 위 새 체크아웃으로 이동하도록
     리다이렉트 대상만 변경. **다운그레이드는 결제가 필요 없어 손대지 않음** — 기존
     `/api/billing/checkout` + `/api/billing/verify` 경로 그대로 유지.
   - 크레딧 구매(`credits-purchase-modal.tsx`)는 이미 `hasBillingKey` 분기가 있어 손볼
     필요 없었음 — 신규 구독자가 이제 항상 빌링키를 등록하게 되므로 자연스럽게 크레딧
     구매도 대부분 이 경로를 타게 됨.
   - 타입체크·빌드 통과 확인, main 배포 완료. **[사용자 확인 필요]** 실제 화면에서 새
     플랜 가입 플로우(카드 등록 → 즉시 결제) 테스트 필요.
   **버그 수정(2026-07-21) — 위 신규 구독 실결제 테스트 중 발견: `chargeBillingKey()` 요청
   포맷 오류로 카드 등록은 성공하나 즉시 청구가 항상 실패.** 실사용자가 기본 플랜(할인코드
   YM2026 적용, ₩4,720)으로 실제 테스트한 결과 카드 등록(KB국민카드)까지는 성공했으나
   직후 청구 단계에서 `Invalid value for: body Reason: missing required field "billingKey"`
   에러 발생. 원인은 `lib/billing/portone-provider.ts`의 `chargeBillingKey()`가 포트원 V2
   빌링키 결제 API(`POST /payments/{paymentId}/billing-key`)에 보내는 요청 본문을
   `{ payment: { billingKey, orderName, customer, amount, currency } }`처럼 `payment` 객체로
   한 번 더 감싸서 보내고 있었음 — 이 API는 `billingKey`/`orderName`/`customer`/`amount`/
   `currency`를 **최상위 필드**로 요구하므로, 최상위엔 `billingKey`가 없다는 에러가 난 것.
   `payment` 래핑을 제거해 필드를 최상위로 평탄화하는 것으로 수정. 이 함수는 신규 구독
   결제(`app/api/billing/subscribe/route.ts`)뿐 아니라 크레딧 구매(`app/api/billing/credits/
   charge-billing-key/route.ts`)와 매달 자동갱신 크론(`app/api/cron/subscription-check/
   route.ts`)에서도 공유해서 쓰는 함수라, 오늘 라이브 MID 연동 전까지는 실제로 청구가
   실행된 적이 없어 이 버그가 지금까지 발견되지 못하고 있었던 것으로 보임 — 세 호출부
   모두 이번 수정으로 함께 해결됨. 타입체크·빌드 통과 확인.
   **버그 수정 2(2026-07-21) — 위 수정 직후 재테스트에서 또 다른 원인으로 실패.** 카드
   등록은 성공했지만 브라우저에 "Failed to execute 'json' on 'Response': Unexpected end of
   JSON input" 에러 발생. 원인은 `subscriptions` 테이블 — `activateSubscription()`이
   `upsert(..., { onConflict: 'user_id' })`로 쓰는데 `032_billing_subscriptions.sql`에는
   `user_id`에 일반 인덱스만 있고 **UNIQUE 제약이 애초에 누락**되어 있었음. PostgREST
   upsert의 onConflict가 동작하려면 해당 컬럼에 유니크 제약이 반드시 있어야 하므로 DB
   에러가 났는데, `app/api/billing/subscribe/route.ts`엔 이 예외를 잡는 try/catch가 없어
   Next.js가 빈 응답(500)을 반환했고 클라이언트가 그걸 JSON으로 파싱하려다 저 에러가 난
   것 — 진짜 원인(구독 활성화 실패)이 화면엔 전혀 드러나지 않는 상태였음.
   `activateSubscription()`은 기존 `/api/billing/verify` 경로에서도 똑같이 호출되던
   함수인데, 지금까지 실제 라이브 결제로 신규 구독이 활성화된 적이 이번이 처음이라(그 전
   테스트는 전부 어드민 플랜 강제 변경으로 우회) 이 버그도 여태 발견되지 못했던 것으로
   보임. 조치:
   - `supabase/migrations/059_subscriptions_user_id_unique.sql`(신규) — 혹시 모를 중복
     user_id 행을 먼저 정리한 뒤 `subscriptions.user_id`에 UNIQUE 제약 추가. **[사용자
     액션 필요] Supabase SQL Editor에서 실행 전까지는 구독 활성화가 계속 실패함.**
   - `app/api/billing/subscribe/route.ts` — 실제 처리 로직을 `handleSubscribe()`로 분리하고
     POST 핸들러에서 try/catch로 감싸 예상 못한 예외도 항상 에러 메시지가 담긴 JSON으로
     반환하도록 수정(실결제 라우트에서 빈 응답이 나가는 걸 방지, 원인 파악 가능하게).
   타입체크·빌드 통과 확인, main 배포 완료. **[사용자 확인 필요]** 위 마이그레이션 실행
   후 재테스트 필요.
   **[사용자 액션 필요] 정산준비 가맹점 → 정산 가맹점 전환**: KPN 메일에 "가입하신 보증보험
   서류를 회신 메일로 보내면 확인 후 정산 가맹점으로 전환"이라는 안내가 있어 별도로 서류 제출
   필요. 카카오페이·토스페이 추가 연동 시 신용카드(인증)으로 추가 카드사 심사가 별도로 필요하다는
   점도 메일에 명시됨.
   승인이 사실상 임박했다고 판단해 NHN KCP 백업 트랙은 일시 중단하기로 결정함 (위 2번 참고).
   **통신판매업 신고도 지금은 서두르지 않기로 함** — 실결제 테스트가 이제 막 가능해지는
   시점이라 "직전년도 통신판매 거래횟수 50회 미만" 면제 요건에 해당할 가능성이 높음. 실거래가
   쌓이기 시작하면 50회에 도달하기 전에 여유 있게 신고 진행하는 것으로 미룸.
5. **모바일 UX 개선** — 음성 입력, 결과 공유 등
6. 시연회(7/23·7/24·8/3) 전 전체 기능 점검 — 진행 중(2026-07-20, 코드 레벨 점검은 Claude가,
   화면 클릭 테스트는 사용자가 직접 나눠서 진행)
7. **[아이디어, 2026-07-17, 8/3 시연회 종료 + PG 실결제 안정화 이후 착수] 사용자 피드백 자동
   분석 파이프라인 + 서브에이전트 자동화 4종.** 지금 당장(시연회 전)이 아니라 이 시점까지
   미루기로 한 이유: ①지금은 KPN 실운영 키 설정·결제 테스트·시연회 준비가 최우선순위라
   주의가 분산되면 안 됨, ②아래 4개 중 3개(결제 이상탐지/피드백 분석/사용량 리포트)는
   실제 결제·사용자 데이터가 어느 정도 쌓여야 의미가 있는데 시연회 전엔 그 데이터 자체가
   없음 — 지금 만들어봐야 빈 데이터 대상으로 만드는 셈이라 시연회 이후 실사용자 데이터가
   쌓인 뒤에 만드는 게 더 정확하고 효율적. 실사용자가 늘어난 뒤 매번 사람이 피드백을
   일일이 확인하지 않아도 되도록, 다음 구조로 자동화하기로 논의함:
   - 새 프로그램/별도 서버를 만드는 게 아니라, **기존 저장소 + 예약 실행(Routine)**으로 구현.
     정해진 주기(예: 매주)마다 Claude Code 세션이 자동으로 깨어나 피드백/평가 테이블
     (`feedback`, AI 결과물 평가 등 기존 테이블)을 조회·분석
   - 반복되는 버그 vs 단순 불만 vs 아이디어성 피드백을 구분해서 패턴 파악
   - **명확하고 리스크 낮은 버그**는 코드 수정안까지 미리 준비(PR 형태)
   - **애매하거나 정책 판단이 필요한 피드백**은 요약해서 사용자에게 질문
   - **실제 main 배포는 사용자 확인 후 진행** — 초반엔 완전 자동 배포 대신 사람 승인 게이트를
     유지하기로 함(유료 고객이 쓰는 서비스라 자동 배포 리스크가 크다고 판단). 신뢰가 쌓이면
     이후 단계적으로 자동화 범위 확대 검토
   - 구현 시 확인 필요: 이 세션이 도는 샌드박스 환경에서 프로젝트 자체 Supabase REST
     엔드포인트(`xasqlshbvganvczsfkdi.supabase.co`)로 HTTPS 접근이 가능한지 — 같은 날
     `api.supabase.com`/`api.vercel.com` 같은 외부 관리 API 호스트는 프록시 정책상 차단되는
     것을 확인했으나(위 참고), 프로젝트 자체 API 엔드포인트는 별개일 수 있어 실제 구현 시
     별도 테스트 필요
   - **(2026-07-17 추가 논의) 같은 방식(예약 실행 서브에이전트)으로 확장 가능한 추가 아이디어**:
     1. **배포 전/주기적 스모크 테스트 봇** — 지금까지 실제로 겪은 버그 대부분이 "실사용자가
        재테스트하다가 발견"하는 패턴이었음(푸시알림 7단계 원인, 문자 파싱 버그 등). 테스트
        계정으로 회원가입→AI 생성→결제 같은 핵심 플로우를 서브에이전트가 주기적으로 직접
        실행해보고, 깨진 게 있으면 실사용자보다 먼저 발견해서 알리는 구조. 우선순위 가장 높음
     2. **결제/구독 이상 탐지** — 빌링키 자동청구 실패, 구독 다운그레이드 처리 누락, 할인코드
        어뷰징 패턴 등을 주기적으로 스캔해서 이상 징후만 보고. 실결제가 곧 시작되는 시점이라
        중요도 높음
     3. **사용량/매출 정기 리포트 자동화** — 지금은 요청할 때마다 수익률표를 만들어주는데,
        이걸 주기적으로(매주/매월) 먼저 만들어서 보내주는 방식으로 전환
   - **공통 원칙**: 완전 무인 자동화가 목표가 아니라, "사람이 미리 다 확인해두고 요약/승인
     요청만 사용자에게 주는" 구조를 유지하기로 함 — 특히 배포·결제처럼 되돌리기 어렵거나
     돈이 걸린 영역은 계속 사용자 확인을 거치는 게 안전하다고 판단.
8. **[아이디어, 2026-07-17, 8/3 시연회 종료 + PG 실결제 안정화 이후 착수] 재사용 가능한
    "스타터 템플릿" 저장소 분리.** 사용자가 올해 추가로 다른 프로그램 4~5개를 더 만들
    계획이라, FP AI Assistant에서 이미 검증된 뼈대(Supabase Auth/profiles 구조, 포트원
    결제 연동, `lib/subscription/plans.ts`+`usage.ts` 사용량 한도 시스템, 초과 크레딧
    구매, 할인코드 시스템, 관리자 대시보드)를 별도 저장소(예: `fp-saas-starter`)로 추출해
    다음 프로그램부터는 인증·결제·한도 관리를 처음부터 새로 짜지 않고 재사용하기로 함.
    보험설계사 도메인 특화 로직(AI 스크립트/문자/뉴스레터 생성, 상품 카테고리, 고객관리)은
    제외하고 뼈대만 분리. 진행 순서: ①뼈대 파일 식별·복사 ②도메인 특화 부분은 예시 기능
    한두 개로 단순화 ③환경변수·마이그레이션 순서·포트원 설정을 README로 문서화
    ④처음부터 clone해서 정상 작동하는지 검증. 시연회 전 지금은 진행하지 않음 — 데모/PG
    안정화가 우선순위이고, 이 작업 자체도 시간이 꽤 드는 별도 작업이라 여유 있을 때
    진행하는 게 맞다고 판단.

---

## 개발 규칙 / 중요 결정사항

- 수익률 표 요청 시: **항상** 서버비 + 결제수수료 + 환율 ₩1,550 포함, 100%/70%/50% 시나리오 모두 표시
- AI 모델: Sonnet 5 (고품질 필요 기능), Haiku (나머지) — 임의로 바꾸지 말 것
- 크레딧 구매: 유료 플랜(basic/pro/premium)만 가능 — free 차단 필수
- 배포: `main` 브랜치 push → Vercel 자동 배포
- 타입체크: 커밋 전 `npx tsc --noEmit` 항상 실행
