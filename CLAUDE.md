# FP AI Assistant — Claude 작업 컨텍스트

> 이 파일은 세션이 바뀌어도 Claude가 프로젝트 맥락을 유지하기 위한 파일입니다.
> 새 기능을 추가하거나 중요한 결정이 생기면 이 파일을 업데이트해주세요.

---

## 프로젝트 개요

- **서비스명**: FP AI Assistant (보험설계사 전용 AI 도구)
- **스택**: Next.js 14 App Router (TypeScript), Supabase Auth + DB, Vercel 배포
- **AI 모델**: Anthropic Claude
  - 기본(대부분 기능): `claude-haiku-4-5-20251001`
  - 고품질(스크립트+성향분석 포함, SMS 5번째 초안): `claude-sonnet-5`
- **결제**: 포트원(PortOne) + 한국결제네트웍스(KPN) — 일반결제/정기결제(빌링키) 코드 구현 완료, PG 계약 심사 중
  (2026-07-13 KPN 카드사 심사 제출, 최장 2주 소요 예상). 승인 불발 대비 **NHN KCP를 백업 PG로 병행
  신청 진행 중** (가입비/연회비 무료 안내 받음) — 포트원이 NHN KCP를 이미 지원하므로 승인되면
  코드 수정 없이 관리자콘솔에서 채널만 추가하면 전환 가능
- **배포 브랜치**: `main` → Vercel 자동 배포

---

## 요금제 (현재 확정)

> **월정액 결제만 지원** — KPN(한국결제네트웍스) 내부 정책상 연간결제 서비스는 PG 입점이
> 불가하다는 안내를 받아, 연간결제/2개월 할인 관련 UI·로직을 전부 제거함 (2026-07-10).

| 플랜 | 월정액 |
|------|--------|
| 무료 | ₩0 |
| 기본 (Basic) | ₩5,900 |
| 프로 (Pro) | ₩9,900 |
| 프리미엄 (Premium) | ₩16,900 |

### 플랜별 기능 한도

| 기능 | 무료 | 기본 | 프로 | 프리미엄 |
|------|------|------|------|---------|
| AI 문자/카톡 | 3 | 15 | 30 | 70 |
| AI 스크립트 통합풀 | 0 | 10 | 20 | 45 |
| AI PDF 분석 (업로드+생성 통합 한도) | 3 | 10 | 30 | 50 |
| SNS·블로그 콘텐츠 | 0 | 0 | 10 | 20 |
| 뉴스레터 | 0 | 0 | 2 | 10 |

> **AI 스크립트 통합풀**은 AI 상담 스크립트·고객 성향분석 2개 기능이 하나의 카운터를 공유함
> (`lib/subscription/usage.ts`의 `'script'` feature)
>
> **뉴스레터 프로 2회**는 의도적인 "맛보기" — 프리미엄의 진짜 차별점은 블로그·SNS 콘텐츠
> 고용량(20 vs 프로 10)이라 뉴스레터를 프로에 소량 개방해도 프리미엄 업셀 유인이 크게
> 줄지 않는다고 판단해 2026-07-13 추가. 뉴스레터 이미지(JPG/PNG) 내보내기 기능의 발행인
> 이름·연락처는 계정 프로필(`profiles.full_name`/`phone`)에 고정되며 패널에서 직접 수정
> 불가 — 계정 하나를 여러 사람이 공유해 각자 다른 이름으로 이미지를 찍어내는 것을 막기
> 위함(`components/newsletter/newsletter-image-panel.tsx`). 이 때문에 회원가입 시 연락처가
> 필수 항목으로 바뀌었고(`components/auth/signup-form.tsx`), 프로필에 연락처가 없는 계정은
> 뉴스레터 이미지 생성이 차단되고 설정 페이지로 안내됨.
> AI 후속연락 추천, 거절극복 스크립트, 재무설계 간이 리포트, AI 상담 후기 작성 기능은 모두 제거됨
> (거절극복은 AI 상담 스크립트 자체의 [OBJECTION] 섹션과 기능이 겹쳐 별도 메뉴로 둘 가치가 낮다고
> 판단, 재무리포트·후기작성은 사용성이 낮고 통합풀 소모만 유발해서 제거)
>
> **AI PDF 분석**은 `pdfUploadLimit`(업로드)과 `pdfAnalysisLimit`(설명자료 생성)이 항상 동일한
> 값으로 설정됨 — PDF 업로드는 로컬 텍스트 추출만 하므로 AI 토큰 비용이 없고(비용은 오직
> "설명자료 생성" 단계에서만 발생), 두 한도를 분리해두면 실제 처리 가능한 PDF 개수가 왜곡되므로
> 항상 같은 숫자로 유지할 것. 추출된 텍스트는 사용자에게 노출하지 않음(미리보기/다운로드 없음) —
> `document_generator`가 업로드된 파일을 선택하면 서버에서 저장된 추출 텍스트를 그대로 프롬프트에 사용.

---

## 수익률 계산 기준 (항상 이 기준으로 계산)

- **환율**: ₩1,550 / USD (고정)
- **서버비**: ₩70,000 / 월 (Vercel + Supabase)
- **결제 수수료**: 3.6% (Toss페이먼츠 신용·체크카드 실제 수수료율, 부가세 별도)
- **AI 모델 비용** (Sonnet 5 할인 중 ~2026-08-31):
  - Sonnet 5: Input $2/1M → ₩3,100/1M / Output $10/1M → ₩15,500/1M
  - Haiku 4.5: Input $0.80/1M → ₩1,240/1M / Output $4/1M → ₩6,200/1M
- **사용 시나리오**: 100% / 70% / 50% 3가지 모두 항상 표시

---

## 초과 크레딧 (추가 건수 구매) 시스템

유료 플랜 전용, 한도 소진 후 추가 구매 가능. 30일 유효.

| 팩 | 금액 | 건당 단가 | 할인 | 배지 |
|----|------|-----------|------|------|
| 10건 | ₩2,000 | ₩200 | 기준 | - |
| 20건 | ₩3,800 | ₩190 | -5% | - |
| 30건 | ₩5,400 | ₩180 | -10% | POPULAR |
| 40건 | ₩6,800 | ₩170 | -15% | - |
| 50건 | ₩8,000 | ₩160 | -20% | BEST |

- DB 테이블: `user_extra_credits`
- 팩 정의 파일: `lib/billing/credit-packs.ts`
- 크레딧 로직: `lib/subscription/usage.ts` (getExtraCredits / consumeExtraCredit / addExtraCredits)
- 구매 API: `app/api/billing/credits/purchase/route.ts`
- 구매 모달: `components/billing/credits-purchase-modal.tsx`

---

## 할인코드(시연회 프로모션) 시스템

시연회(7/23·7/24, 8/3) 참석자 한정 할인코드. 결제 기존 로직은 건드리지 않고 체크아웃
시점에 선택적으로 코드를 입력한 경우에만 참조하도록 별도 테이블로 추가함 (2026-07-13).

| 코드 | 사용 예정일 | 할인율(기본/프로 · 프리미엄) | 기간 | 선착순 | 유효기한 |
|------|------------|---------------------------|------|--------|----------|
| `MET2026` | 7/23, 7/24 | 20% · 30% | 최초 3개월 | 100명 | 2026-08-10 |
| `YM2026` | 8/3 | 20% · 30% | 최초 3개월 | 100명 | 2026-08-10 |

- 두 코드 합쳐 최대 200명, **계정당 1회만** 사용 가능
- 할인은 자동결제(빌링키) 등록 기반 정기결제에만 적용 — 3개월(회차) 지나면 자동으로 정상가 전환
- DB 테이블: `discount_codes`, `discount_code_redemptions` (RLS `using(false)`, 서비스롤 전용)
- 검증/적용 로직: `lib/billing/discount.ts` (validateDiscountCode / redeemDiscountCode / getActiveRedemption)
- 결제 검증(`app/api/billing/verify/route.ts`)에서 클라이언트가 보낸 할인가를 서버에서 재검증 —
  플랜 정가 검증과 동일한 패턴으로 위변조 방지
- 갱신 결제 시 할인 재적용: `app/api/cron/subscription-check/route.ts`에서 `getActiveRedemption`으로
  남은 회차 확인 후 동일 할인율 적용, 성공 시 `incrementRedemptionUsage` 호출
- 체크아웃 UI: `components/billing/portone-checkout.tsx`의 할인코드 입력란

---

## 법적 보호 조치

- [x] **프로그램저작권 등록 신청 완료 (2026-07-16)** — 한국저작권위원회 저작권등록시스템(cros.or.kr)에
      개인 명의로 신청, 결제(53,600원, 신용카드)까지 완료. 접수번호 `2026-043600`. 심사 대기 중
      (형식심사, 통상 1개월 이내). 진행 상황은 cros.or.kr "나의저작권 > 신청진행현황"에서 확인.
  - AI(Claude Code) 활용 개발이라 "생성형 인공지능(GAI) 활용 저작물의 저작권 등록 안내서"
    (문화체육관광부·한국저작권위원회, 2025.6) 기준에 맞춰 신청 — 순수 GAI 산출물이 아니라
    저작자(사용자)가 기획·설계·정책 결정·오류 검증·수정 지시를 직접 수행하고 GAI는 코드
    구현 도구로 활용했다는 점을 "본 프로그램의 특징"란 및 첨부 서류(GAI 활용 내역 설명서)에
    명시함 — 안내서가 요구하는 통제가능성·예측가능성 요건 충족 목적
  - 프로그램 종류: 응용프로그램 > 사무관리 > 기타 사무관리 S/W, 판매구분: 상업용
  - 첨부 자료(스크래치패드에 생성): 프로그램 명세서(.docx), 소스코드 발췌본(.zip —
    script/message route, provider.ts, request-lock.ts, usage.ts, discount.ts 6개 핵심
    파일), GAI 활용 내역 설명서(.docx, ①개요 ②활용된 GAI 산출물 ③인간의 창작적 표현
    부분 3단계 구조)
- [x] **이용약관 v1.2 → v1.3 (2026-07-16)** — 지식재산권(제9조)·금지행위(제10조) 조항 신설.
      기존 약관에 서비스 자체(UI/디자인/소스코드/AI 프롬프트)에 대한 권리 귀속, 크롤링·역공학·
      재판매·계정 공유 금지 조항이 없었던 걸 보완. 자세한 내용은 `app/terms/page.tsx` 참고.
- [ ] **NHN KCP 백업 PG 신청** — 진행 상황 확인 필요(사용자가 KPN 카드사 심사 통과 못할 경우를
      대비해 병행 신청 중이라고 언급, 정확한 진행 단계는 미확인)
- [ ] 상표 출원 — 시연회 반응 보고 판단하기로 함 (당장 급하지 않음)

---

## 인프라 현황

- [x] Vercel Pro 업그레이드 완료 (2026-07-13) — Hobby 플랜의 상업적 이용 금지 약관 위반 리스크 및
      함수 타임아웃(10초→300초) 문제 해소. 프리미엄 성향분석 스크립트(Sonnet)는 응답까지 약 120초
      걸려 Hobby 플랜에서는 타임아웃으로 실패했을 가능성이 있었음
- [x] **버그 수정(2026-07-15)**: 위 업그레이드 이후에도 실사용자가 "고객성향분석 결과 불러오기"를
      적용해 AI 상담 스크립트를 생성하면 한참 로딩하다가 결과 없이 끝나는 문제 발생. 원인은
      `app/api/ai/script/route.ts`에 `export const maxDuration = 60`이 하드코딩돼 있어서, Vercel
      Pro의 기본 300초 한도와 무관하게 이 라우트만 60초에서 강제 종료되고 있었음 — 성향분석
      연동 시(Sonnet + 16,000토큰 상세 프롬프트) 실측 약 120초가 걸리므로 항상 타임아웃남.
      240초로 상향 수정.
- [x] Supabase Pro 업그레이드 완료 (2026-07-13) — 무료 플랜의 7일 비활성 자동 일시정지 리스크 해소
- [x] Anthropic API 크레딧 자동충전 설정 완료 (잔액 $15 이하 시 $50 자동 충전)
- 100명 규모 기준으로는 Vercel/Supabase Pro 기본 포함 한도 안에서 충분히 커버됨 (자세한 근거는
  대화 기록 참고, 필요 시 재계산 가능)

---

## 포트원(PortOne) 결제 연동 현황

`lib/billing/portone-provider.ts` 에 상세 주석 있음. PG는 한국결제네트웍스(KPN)로 신청,
가입비/연회비 무료 + 빌링·수기 수수료 2.90%(부가세 별도, 계약 확정 전 잠정치).

- [x] 일반결제(요금제/크레딧 1회 결제) — `PortOne.requestPayment()`, 리다이렉트 없이 응답 즉시 확인
- [x] 빌링키 발급/자동청구/삭제 — `verifyBillingKey`/`chargeBillingKey`/`deleteBillingKey`
- [x] profiles 컬럼: `portone_billing_key`, `portone_customer_id`, `billing_card_last4`, `billing_card_brand`
- [x] 구독 만료 크론에서 빌링키 자동 갱신 시도 (`app/api/cron/subscription-check/route.ts`)
- [ ] **PG 계약 승인 후 남은 작업**: Vercel에 `PORTONE_API_SECRET`, `NEXT_PUBLIC_PORTONE_STORE_ID`,
      `NEXT_PUBLIC_PORTONE_CHANNEL_KEY`, `PORTONE_WEBHOOK_SECRET` 실제 값 설정 → 실결제 테스트
- [ ] 확정된 최종 수수료율을 이 문서 "수익률 계산 기준" 절의 3.6%와 대조해 필요 시 갱신

---

## 주요 구현 완료 기능 목록

### AI 기능
- [x] AI 문자/카톡 생성 (Haiku 4가지 + Sonnet 1가지("설득력 있는 버전"), 5가지 스타일).
      **버그 수정(2026-07-16)**: "부드러운 버전"에 "후속 연락용" 내용이 그대로 섞여 나오는
      문제 발생 — `parseOutputSections`가 Haiku 응답(SMS/KAKAO/SOFT/FOLLOWUP 4개 마커)과
      Sonnet 응답(PERSUASIVE 1개 마커)을 하나의 고정 마커 순서로 같이 파싱하다가, Haiku
      텍스트엔 없는 PERSUASIVE 마커를 못 찾아 SOFT 섹션이 텍스트 끝(FOLLOWUP까지)을 통째로
      삼켜버렸음. 각 raw 텍스트를 실제로 생성한 프롬프트의 마커 목록으로만 파싱하도록 수정
      (`app/api/ai/message/route.ts`). 부수적으로 Haiku maxTokens 2500이 4개 섹션 합산
      분량엔 부족해 마지막 섹션이 잘리던 문제도 5000으로 상향해 같이 해결. "설득력 있는
      버전"(Sonnet)도 별도로 문장 중간 잘림 발생 이력 있어 maxTokens 3000→4096 상향, 목표
      분량도 사용자 요청으로 700~1000자→1200~1500자 확대(추가 비용 생성 1건당 약 15~16원
      수준, 무시 가능). **추가 버그 수정(2026-07-16)**: 위 수정 직후 4가지 버전 전부에서
      문단 앞마다 갑자기 ①②③④⑤ 번호가 붙고 말미에 `---`/`##` 마크다운 잔재가 남는 문제
      발생 — 프롬프트의 "5단계 구조" 설명에 원문자 번호(①②③④⑤)를 그대로 써둔 게 원인으로,
      모델이 이를 실제 출력 서식으로 오해해 그대로 베껴 씀. 설명 문구를 원문자 번호 없는
      화살표 흐름으로 바꾸고, 번호·글머리 기호·마크다운 금지를 명시했으며(이 프롬프트에는
      script/newsletter 라우트와 달리 `---`/`##` 금지 지시 자체가 없었음), 파싱 단계에도
      `cleanSectionText` 방어 로직 추가.
- [x] AI 상담 스크립트 생성 (성향분석 포함 시 Sonnet 5, 미포함 시 Haiku)
- [x] 고객 성향 분석 (Sonnet 5, 프로 이상). "기존 고객 불러오기" 드롭다운으로 고객관리에
      등록된 고객을 선택하면 이름·나이대·성별·직업·가족상황이 자동으로 채워짐(2026-07-15).
      고객관리와 성향분석은 값 체계가 달라(연령대 세분화, 소득 구간 경계 등) 안전하게
      대응되는 항목만 자동 채우고, 소득 수준은 구간이 어긋나서 자동 채우지 않음
      (`components/ai/customer-analysis.tsx`의 `handleSelectCustomer`).
- [x] AI PDF 분석 (Haiku, 3개 그룹 병렬 호출)
- [x] 블로그·SNS 콘텐츠 생성 (Haiku, 프로 이상)
- [x] 뉴스레터 생성 (Haiku, 프로 2회/프리미엄 10회). 주제 입력은 자유 타이핑 대신 **대분류 7개
      (건강/의료, 금융/재테크, 보험상식/점검, 생활/시즌이슈, 생활안전/사고예방, 법인/전문직특화,
      고객관계관리·감사콘텐츠) → 중분류(대분류당 4~5개) → AI 주제 제안(최대 3회, 사용량 차감 없음)
      → 선택 또는 직접입력/수정** 흐름으로 개편(2026-07-15) — 신입 설계사의 "뭘 써야 할지 모르겠다"
      문제 해결이 목적. `lib/content/newsletter-topics.ts`(카테고리 데이터),
      `app/api/ai/newsletter/suggest-topics/route.ts`(제안 전용 API — `blockIfLimitExceeded`/
      `incrementUsage`를 호출하지 않아 뉴스레터 생성 한도와 무관하게 재시도 가능, 프론트에서
      3회로 제한). **"계절/시기 이슈" 자유입력 필드는 삭제** — 서버가 오늘 날짜로 자동 계산
      (`lib/content/newsletter-season.ts`)해서 프롬프트에 반영. 실시간 검색(정책/제도 변화 카테고리)은
      웹서치 API 비용($10/1,000회 ≈ 1회당 ₩15.5) 문제로 채택하지 않고 "고객 관계관리·감사 콘텐츠"로
      대체 — 검색 없이도 항상 안전한 비용으로 생성 가능한 카테고리만 채택.
      카테고리만 선택하고 "참고 자료"를 안 채우면 내용이 다소 일반적으로 나올 수 있어 눈에 띄는
      안내 박스 추가(2026-07-15). **발행인 로고/프로필 사진**은 설정 페이지에서 계정당 1장 등록
      → 모든 뉴스레터 이미지 하단 발행인 정보 옆에 자동 표시(이름·연락처와 동일하게 계정 공유
      방지 원칙 적용, 이미지 패널에서는 수정 불가). `profiles.avatar_url` +
      `newsletter-avatars` 공개 스토리지 버킷(RLS: 본인만 업로드/수정/삭제, 전체 공개 읽기 —
      html-to-image가 브라우저에서 직접 불러와야 해서). **[사용자 액션 필요]**
      `supabase/migrations/055_newsletter_publisher_image.sql` 아직 미실행 — Supabase SQL
      Editor에서 실행 전까지는 로고 업로드 시 에러 발생.
- [x] 뉴스레터 이미지(JPG/PNG) 내보내기 — 레이아웃 6종, 폰트 3종, 발행인 정보 계정 고정.
      보관함(내 결과물)에서 재생성 시 사용량 차감 없음(`parseNewsletterSections`로 저장된
      텍스트를 역파싱). 보험 점검 포인트는 항상 정확히 3개, "✓ " 한 줄 후킹 문장으로 고정
      (`app/api/ai/newsletter/route.ts` 프롬프트). 마크다운 기호(#, ##, **, 트레일링 --- 등)는
      프롬프트에서 금지 + 이미지 조립 최종 단계(`newsletter-image-panel.tsx`의
      `clean = stripTrailingRule(stripMarkdown(text))`)에서 한 번 더 방어적으로 제거 —
      과거 생성분을 보관함에서 다시 불러와도 깨끗하게 나옴. CTA에 사과 표현("그동안 안부
      문자로만...") 금지 규칙도 프롬프트에 명시. 컴플라이언스 주의문구는 텍스트/TXT
      다운로드에는 남기되 이미지에는 `stripDisclaimer()`로 제외.
      **프롬프트 수정(2026-07-14) 이전에 저장된 보관함 항목**은 체크포인트 5개·사과 표현이
      원본 그대로 저장돼 있어 재생성해도 그대로 나오는 문제가 있었음 — `limitCheckPoints()`
      (몇 개든 이미지에는 앞 3개 항목 단위만 표시), `stripApologyCta()`("안부 문자"+"죄송/미안"
      패턴 문장만 제거)를 이미지 조립 단계에 추가해 옛 저장분도 이미지에서는 깔끔하게 나오도록
      방어. 원본 저장 텍스트·TXT/DOCX 다운로드는 그대로 유지(컴플라이언스 문구와 동일 원칙).
- [x] 템플릿 라이브러리 19종 (문자 10 + 스크립트 9, 프리미엄 전용 4종 포함) — `content_templates` 테이블
- [x] 생애주기 알림 (구현 중)
- [x] 오늘의 동기부여 명언 팝업 — 대시보드 진입 시 하루 1회, 날짜 기준으로 전 사용자
      공통 노출(같은 날엔 모두 같은 명언). 검증된 인물 명언 30개 + 직접 작성한
      보험 세일즈 특화 문구 93개, 총 123개 순환. `lib/content/sales-quotes.ts`,
      `components/dashboard/daily-quote-popup.tsx`. 노출 여부는 localStorage로만
      관리 — DB/서버 비용 없음, 기존 로직과 완전 분리된 추가 기능 (2026-07-14)

### 결제/구독
- [x] 요금제 페이지 (기본/프로/프리미엄)
- [x] 포트원(PortOne) 일반결제/빌링키 연동 구조 (PG 계약 승인 전)
- [x] 초과 크레딧 5개 팩 구매 플로우
- [x] 70%/90% 사용량 경고 배너
- [x] 한도 초과 배너 + 크레딧 구매 버튼

### UX
- [x] 음성 입력 (AI 문자, 스크립트, 성향분석, PDF 설명자료, 블로그 핵심메시지)
- [x] 내 결과물 보관함 저장 (스크립트, 콘텐츠 등)
- [x] 대시보드 사용량 카드 (플랜별 기능 자동 표시)
- [x] 플랜 잠금 (PlanGate 컴포넌트)

### 인프라
- [x] Supabase DB 마이그레이션 (usage_records, user_extra_credits 등)
- [x] 어드민 페이지 (사용자 관리, 빌링, CSV 내보내기)
- [x] 팀 관리 기능 — 팀 생성(팀장)은 **프리미엄 전용**, 팀원 연결은 **기본 플랜 이상만** 가능
      (2026-07-15 수정 — 이전엔 플랜 체크가 전혀 없어 무료 회원도 팀을 만들고 초대할 수 있던 버그).
      **팀원 한도 대여**: 팀원이 본인 월 사용량을 다 쓰면, 소속 팀장의 남은 한도에서 자동으로
      대신 차감됨(`lib/subscription/usage.ts`의 `reserveUsage()`) — **문자/스크립트(성향분석
      포함)/PDF분석 3개만** 대상. 콘텐츠·뉴스레터는 대여 대상에서 제외(2026-07-15) — 기본
      플랜은 이 두 기능이 원래 0회(프로 이상 전용)라 대여를 허용하면 요금제 등급이 무력화되고,
      뉴스레터는 발행인 이름·연락처가 항상 생성한 본인 것으로 찍히는 특성상 팀장 한도로 팀원
      개인 브랜딩 뉴스레터를 만드는 것도 맞지 않다고 판단. 팀장 본인 한도까지 소진되면 그때는
      정상적으로 한도 초과 처리.
      생성된 결과물은 차감 주체와 무관하게 항상 실제 생성한 팀원 본인의 "내 결과물 보관함"에
      저장됨. `getMonthlyUsage()`/`getCurrentUserPlan()`은 RLS(`auth.uid()=user_id`) 우회를 위해
      admin client로 조회하도록 수정 — 팀원 세션에서 팀장의 사용량을 확인해야 하는 이 기능
      특성상 필요했음(기존 "본인 조회" 호출부는 결과 동일, 하위 호환 확인됨). 기존에 이미
      생성된 팀/팀원은 소급 적용되지 않고 그대로 유지됨(신규 생성·연결 건부터 적용).
- [x] 백업 로그 시스템

---

## 앞으로 할 것 (우선순위 순)

1. **[검증 대기] 일정 알림(푸시) 안 오는 문제 — 2026-07-14~15에 걸쳐 원인 7개를 순서대로 발견·수정.**
   실제 발송 테스트를 반복하며 원인이 하나씩 드러난 케이스라, 최종 진짜 원인과 함께 전부 기록:
   - **VAPID 키**: Vercel Production 환경변수에 `NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/
     `VAPID_EMAIL` 3개 모두 정상 등록되어 있음을 대시보드에서 직접 확인(2026-07-15) — 처음 의심했던
     이 원인은 아니었음. 키 분실 시 `node -e "console.log(require('web-push').generateVAPIDKeys())"`로
     재생성 가능(재생성하면 기존 구독 전부 무효화되므로 최후의 수단으로만).
   - **크론 주기**: 하루 1회(오후 6시 KST)뿐이라 그 전 시간대 알림은 원천적으로 못 갔던 문제 —
     처음엔 15분 간격으로 수정했으나, "10분 전" 같은 짧은 알림 설정과 어긋나 실제 발송이
     마감 직전 2~3분 전으로 밀리는 오차가 있어 **1분 간격(`* * * * *`)으로 재조정**
     (Vercel Pro 플랜이라 분 단위 크론 가능, `vercel.json`).
   - **9시간 시차 버그**: `app/api/cron/push-notify/route.ts`에서 `due_date`+`due_time`을 시간대
     표기 없이 `new Date()`로 파싱하면 Vercel 서버(UTC) 기준으로 해석돼, 오후 2:30 설정이 서버에는
     밤 11:30(UTC 14:30)으로 잘못 인식되던 문제. `+09:00` 오프셋 명시로 수정 완료.
   - **구독 토글이 꺼져 있었음**: 사용자가 실제로 설정 페이지에서 "일정 알림" 토글이 꺼진 상태였음
     — 다시 켜서 구독 재생성.
   - **★ 진짜 최종 원인(2026-07-15)**: `notify_before_minutes`(몇 분 전에 알릴지)가 **일정 생성/수정
     API(`app/api/tasks/route.ts` POST, `app/api/tasks/[id]/route.ts` PATCH)에서 아예 저장 필드
     목록에 빠져 있어서, 프론트엔드는 정확히 값을 보내는데 서버가 계속 버리고 항상 NULL로만
     저장하고 있었음**. 크론은 `notify_before_minutes IS NOT NULL` 조건으로 대상을 찾기 때문에,
     위 4가지를 전부 고쳐도 애초에 알림 대상 자체가 하나도 없어 발송이 안 됐던 것 — 이게 처음부터
     끝까지의 진짜 근본 원인. TASK_SELECT에도 추가해서 수정 폼 재오픈 시 기존 알림 설정이 안 보이던
     문제도 같이 해결. 부수적으로 PATCH에서 `customer_id`("고객 미연결" 선택 시 빈 문자열)를 그대로
     DB에 보내 "invalid input syntax for type uuid" 에러가 나던 버그도 함께 발견·수정(생성 흐름은
     이미 `|| null` 정규화가 있었는데 수정 흐름에만 빠져 있었음).
   - 아이폰은 **"홈 화면에 추가"로 설치한 상태에서만** 푸시 알림이 동작함(iOS 자체 제한, Safari
     탭으로 열면 절대 안 됨) — 사용자에게 매번 안내 필요.
   - **6번째 원인(2026-07-15)**: 위 5가지를 전부 고친 뒤에도 실사용자가 재테스트 2회 모두 알림을
     못 받았음. 원인은 크론 실행 주기(15분 간격, `*/15 * * * *` → :00/:15/:30/:45에만 실행)와
     알림 가능 구간의 폭 차이 — "10분 전" 같은 짧은 알림을 설정하면 `[notifyAt, dueAt]` 구간이
     15분보다 좁아서, 크론이 그 구간을 완전히 비껴가면(예: 구간이 12:03~12:13인데 크론은
     12:00/12:15에만 실행) `now > dueAt`이 돼버려 영원히 스킵되고 다시는 안 옴. `notify_before_minutes`
     저장은 정상 동작해도 발송 자체가 크론 타이밍에 의해 누락될 수 있었던 구조적 문제.
     `app/api/cron/push-notify/route.ts`에서 마감 시각 이후 30분 유예(`GRACE_MS`)를 두어 다음
     크론에서 반드시 잡아내도록 수정 — 정시보다 늦게 오더라도 아예 안 오는 것보다 낫다는 판단.
     발송 문구도 설정값(`notify_before_minutes`) 대신 발송 시점에 실제 남은 시간을 계산해서
     표시하도록 변경(유예 시간 때문에 "10분 후"라고 보냈는데 이미 지난 경우를 방지).
   - **★★ 진짜진짜 최종 근본 원인(2026-07-15) — 크론 요청이 미들웨어에서 /login으로
     리다이렉트되고 있었음**: 위 6가지를 전부 고치고도 재테스트 2회 모두 실패해서 사용자가
     요청한 Vercel Cron Jobs 실행 로그를 확인한 결과, `/api/cron/push-notify` 요청이 매번
     `GET 307`(리다이렉트)로 찍히고 있었음 — 즉 핸들러 코드 자체가 한 번도 실행된 적이
     없었음. 원인은 `proxy.ts`의 로그인 여부 체크: Vercel Cron이 보내는 요청은 브라우저
     세션 쿠키가 없으므로(각 크론 라우트는 대신 `Authorization: Bearer <CRON_SECRET>`로
     자체 인증하도록 설계돼 있었음) 미들웨어가 매번 `user === null`로 판정해 `/login`으로
     307 리다이렉트시키고 있었고, 그 요청은 실제 알림 발송 로직에 도달하지도 못했음. 위
     6가지 원인은 전부 실재하는 버그였지만, 이 미들웨어 문제 때문에 애초에 그 코드가 실행될
     기회 자체가 없었던 것 — 이게 처음부터 끝까지의 최종 근본 원인. `proxy.ts`에서
     `/api/cron/*` 경로는 세션 체크를 건너뛰고 각 라우트 자체 인증에 맡기도록 수정.
     **주의**: `/api/cron/subscription-check`(구독 갱신·빌링키 자동청구)와
     `/api/cron/cleanup-files`도 동일한 미들웨어를 거치므로 똑같이 실행되지 않고 있었을
     가능성이 높음 — 결제 PG 승인 후 실결제 테스트 시 구독 자동갱신이 정상 동작하는지
     반드시 별도 확인 필요.
   - 위 7가지 모두 수정 완료 상태로 실사용자가 재테스트 예정 — 결과 확인 대기. 기존 테스트
     일정은 이미 알림 구간이 지나 재발송되지 않으니, 재테스트 시 반드시 새 일정으로 할 것.
     Vercel Cron Jobs 로그에서 이제 `GET 200`(더 이상 307 아님)으로 찍히는지, 응답 바디의
     `sent`/`notified` 값이 몇인지로 정상 실행 여부를 1차 확인 가능.
2. **NHN KCP 병행 신청 이어가기** — 포트원에서 "테스트모드 설정 안내" 메일 받고 검토 중이었음.
   결제 서비스 테스트 채널(포트원 관리자콘솔 → 결제연동 → 테스트 → NHN KCP)만 추가하면 되고,
   메일에 같이 언급된 "본인인증 서비스"(다날/KG이니시스) 항목은 이 앱과 무관하니 무시할 것.
3. **KPN 카드사 심사 승인 대기** — 승인 후 Vercel에 실운영 포트원 키
   (`PORTONE_API_SECRET`, `NEXT_PUBLIC_PORTONE_STORE_ID`, `NEXT_PUBLIC_PORTONE_CHANNEL_KEY`,
   `NEXT_PUBLIC_PORTONE_BILLING_CHANNEL_KEY`) 설정 → 실결제 테스트
4. **생애주기 알림 UI 완성** (Task #24 in_progress)
5. **고객 DB 연동** — 고객 정보 저장/불러오기로 스크립트 자동완성
6. **모바일 UX 개선** — 음성 입력, 결과 공유 등
7. 시연회(7/23·7/24·8/3) 전 전체 기능 점검 — 사용자가 직접 테스트 후 필요 시 요청 예정

---

## 개발 규칙 / 중요 결정사항

- 수익률 표 요청 시: **항상** 서버비 + 결제수수료 + 환율 ₩1,550 포함, 100%/70%/50% 시나리오 모두 표시
- AI 모델: Sonnet 5 (고품질 필요 기능), Haiku (나머지) — 임의로 바꾸지 말 것
- 크레딧 구매: 유료 플랜(basic/pro/premium)만 가능 — free 차단 필수
- 배포: `main` 브랜치 push → Vercel 자동 배포
- 타입체크: 커밋 전 `npx tsc --noEmit` 항상 실행
